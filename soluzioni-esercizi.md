---
title: Informatica di Base
subtitle: Università degli Studi di Salerno
---

## Moduli

### 01 | Introduzione alla programmazione e Python

- `[M1-REPL-01]` Successore di 15
	- Testo: Scrivi un'espressione che calcoli il successore di 15.
	- Soluzione:
		```python
		15+1
		```

- `[M1-REPL-02]` Metà del triplo di 12
	- Testo: Scrivi un'espressione che calcoli la metà del triplo di 12.
	- Soluzione:
		```python
		(12*3)/2
		```

- `[M1-REPL-03]` Triplo della metà di 12
	- Testo: Scrivi un'espressione che calcoli il triplo della metà di 12.
	- Soluzione:
		```python
		3*(12/2)
		```

- `[M1-REPL-04]` Resto della divisione tra 137 e 12
	- Testo: Scrivi un'espressione che calcoli il resto della divisione tra 137 e 12.
	- Soluzione:
		```python
		137 % 12
		```

- `[M1-REPL-05]` Doppio della differenza tra 15 e 7
	- Testo: Scrivi un'espressione che calcoli il doppio della differenza tra 15 e 7.
	- Soluzione:
		```python
		2*(15-7)
		```

- `[M1-REPL-06]` Doppio della differenza tra 3 e 7
	- Testo: Scrivi un'espressione che calcoli il doppio della differenza tra 3 e 7.
	- Soluzione:
		```python
		2*(3-7)
		```

- `[M1-REPL-07]` Somma dei primi tre numeri pari
	- Testo: Scrivi un'espressione che calcoli la somma dei primi tre numeri pari.
	- Soluzione:
		```python
		2+4+6
		```

- `[M1-REPL-08]` Media di 2, 5, 9
	- Testo: Scrivi un'espressione che calcoli la media di 2, 5, 9.
	- Soluzione:
		```python
		(2+5+9)/3
		```

- `[M1-REPL-09]` Resto della divisione tra 44 e 7
	- Testo: Scrivi un'espressione che calcoli il resto della divisione tra 44 e 7.
	- Soluzione:
		```python
		44%7
		```

- `[M1-REPL-10]` Somma dei quadrati dei primi tre numeri naturali
	- Testo: Scrivi un'espressione che calcoli la somma dei quadrati dei primi tre numeri naturali.
	- Soluzione:
		```python
		1**2+2**2+3**2
		```

- `[M1-REPL-11]` Lunghezza di "Happy families are all alike"
	- Testo: Scrivi un'espressione che calcoli la lunghezza della stringa "Happy families are all alike".
	- Soluzione:
		```python
		len("Happy families are all alike")
		```

- `[M1-REPL-12]` Versione maiuscola di "Happy families are all alike"
	- Testo: Scrivi un'espressione che calcoli la versione tutta maiuscola di "Happy families are all alike".
	- Soluzione:
		```python
		"Happy families are all alike".upper()
		```

- `[M1-REPL-13]` Concatenazione di due frasi
	- Testo: Scrivi un'espressione che calcoli la concatenazione di "Happy families are all alike" e "every unhappy family is unhappy in its own way".
	- Soluzione:
		```python
		"Ciao"+"mondo!"
		```

- `[M1-REPL-14]` Concatenazione con spazio in mezzo
	- Testo: Scrivi un'espressione che calcoli la stessa concatenazione ma con uno spazio in mezzo.
	- Soluzione:
		```python
		"Ciao"+" "+"mondo!"
		```

- `[M1-REPL-15]` Primo carattere di "informatica"
	- Testo: Scrivi un'espressione che calcoli il primo carattere di "informatica".
	- Soluzione:
		```python
		"informatica"[0]
		```

- `[M1-REPL-16]` Sesto carattere di "supercalifragilistichespiralidoso" in maiuscolo
	- Testo: Scrivi un'espressione che calcoli il sesto carattere di "supercalifragilistichespiralidoso" in maiuscolo.
	- Soluzione:
		```python
		"supercalifragilistichespiralidoso"[5].upper()
		```

- `[M1-REPL-17]` Concatenazione di caratteri specifici di "supercalifragilistichespiralidoso"
	- Testo: Scrivi un'espressione che calcoli la concatenazione del sesto, nono, decimo, ventiseiesimo e ventisettesimo carattere di "supercalifragilistichespiralidoso".
	- Soluzione:
		```python
		"supercalifragilistichespiralidoso"[5]+"supercalifragilistichespiralidoso"[8]+"supercalifragilistichespiralidoso"[9]+"supercalifragilistichespiralidoso"[25]+"supercalifragilistichespiralidoso"[26]
		```

- `[M1-REPL-18]` Quintultimo carattere di "supercalifragilistichespiralidoso"
	- Testo: Scrivi un'espressione che calcoli il quintultimo carattere di "supercalifragilistichespiralidoso".
	- Soluzione:
		```python
		"supercalifragilistichespiralidoso"[-5]
		```

- `[M1-REPL-19]` Carattere centrale di "informatica"
	- Testo: Scrivi un'espressione che calcoli il carattere centrale di "informatica".
	- Soluzione:
		```python
		"informatica"[len("informatica")//2]
		```

- `[M1-PROD-01]` `5 * (2 + 4)`
	- Testo: Che cosa produce l'espressione `5 * (2 + 4)`?
	- Soluzione:
		```python
		30
		```

- `[M1-PROD-02]` `152 % 9`
	- Testo: Che cosa produce l'espressione `152 % 9`?
	- Soluzione:
		```python
		8
		```

- `[M1-PROD-03]` `"banana" + "fragola"`
	- Testo: Che cosa produce l'espressione `"banana" + "fragola"`?
	- Soluzione:
		```python
		"bananafragola"
		```

- `[M1-PROD-04]` `"olio".upper()`
	- Testo: Che cosa produce l'espressione `"olio".upper()`?
	- Soluzione:
		```python
		"OLIO"
		```

- `[M1-PROD-05]` `len("carote")`
	- Testo: Che cosa produce l'espressione `len("carote")`?
	- Soluzione:
		```python
		6
		```

- `[M1-PROD-06]` `"cenerentola"[8]`
	- Testo: Che cosa produce l'espressione `"cenerentola"[8]`?
	- Soluzione:
		```python
		'a'
		```

### 02 | Variabili e strutture decisionali

- `[M2-B1-01]` Saluto con nome e cognome
	- Testo: Leggi nome e cognome e stampa un saluto, per esempio: Ciao, Mario Rossi!
	- Soluzione:
		```python
			nome = input("Nome: ")
			cognome = input("Cognome: ")
			print("Ciao, " + nome + " " + cognome + "!")
		```

- `[M2-B1-02]` Età: anno prossimo e anno scorso
	- Testo: Leggi l'età e stampa: "Tra un anno avrai X anni" e "Un anno fa avevi Y anni".
	- Soluzione:
		```python
			eta = int(input("Età: "))
			print("Tra un anno avrai", eta + 1, "anni")
			print("Un anno fa avevi", eta - 1, "anni")
		```

- `[M2-B1-03]` Operazioni su due interi
	- Testo: Leggi due numeri interi e stampa la loro somma, il loro prodotto e il resto della divisione del primo per il secondo.
	- Soluzione:
		```python
			a = int(input("Primo numero: "))
			b = int(input("Secondo numero: "))
			print("Somma:", a + b)
			print("Prodotto:", a * b)
			print("Resto:", a % b)
		```

- `[M2-B1-04]` Media di tre numeri
	- Testo: Leggi tre numeri e stampane la media.
	- Soluzione:
		```python
			a = int(input("Primo numero: "))
			b = int(input("Secondo numero: "))
			c = int(input("Terzo numero: "))
			print("Media:", (a + b + c) / 3)
		```

- `[M2-B1-05]` Operazioni su una stringa
	- Testo: Leggi una stringa e stampa: la lunghezza, il primo carattere, l'ultimo carattere, i primi tre caratteri.
	- Soluzione:
		```python
		s = input("Stringa: ")
			print("Lunghezza:", len(s))
			print("Primo carattere:", s[0])
			print("Ultimo carattere:", s[-1])
			print("Primi tre caratteri:", s[:3])
		```

- `[M2-B1-06]` Scambio di variabili
	- Testo: Leggi due numeri e stampa prima i valori inseriti e poi gli stessi valori scambiati.
	- Soluzione:
		```python
			a = int(input("Primo numero: "))
			b = int(input("Secondo numero: "))
			print("Valori inseriti:", a, b)
			c = a
			a = b
			b = c
			print("Valori scambiati:", a, b)
		```
	- Soluzione:
		```python
			a = int(input("Primo numero: "))
			b = int(input("Secondo numero: "))
			print("Valori inseriti:", a, b)
			print("Valori scambiati:", b, a)
		```

- `[M2-B1-07]` Nome e anno di nascita
	- Testo: Leggi un nome e un anno di nascita e stampa una frase del tipo: "Ciao Anna, potresti avere 20 o 21 anni".
	- Soluzione:
		```python
			nome = input("Nome: ")
			anno_nascita = int(input("Anno di nascita: "))
			anno_corrente = 2026
			eta = anno_corrente - anno_nascita
			print("Ciao " + nome + ", potresti avere " + str(eta - 1) + " o " + str(eta) + " anni")
		```

- `[M2-B1-08]` Conversione da secondi a ore, minuti, secondi
	- Testo: Leggi un numero intero di secondi (es. 3723) e stampa la conversione nel formato: 1 ore, 2 minuti, 3 secondi.
	- Soluzione:
		```python
			secondi = int(input("Secondi: "))
			ore = secondi // 3600
			minuti = (secondi // 60) % 60
			sec = secondi % 60
			print(ore, "ore")
			print(minuti, "minuti")
			print(sec, "secondi")
		```

- `[M2-B1-09]` Nome e cognome: maiuscolo, minuscolo, iniziali
	- Testo: Leggi nome e cognome. Stampa: il cognome tutto in maiuscolo; il nome con prima lettera maiuscola e resto minuscolo; le iniziali nel formato M.R.
	- Soluzione:
		```python
			nome = input("Nome: ")
			cognome = input("Cognome: ")
			print(cognome.upper())
			print(nome[0].upper() + nome[1:].lower())
			print(nome[0].upper() + "." + cognome[0].upper() + ".")
		```

- `[M2-B1-10]` Stringa senza primo e ultimo carattere
	- Testo: Leggi una stringa e stampala senza il primo e l'ultimo carattere.
	- Soluzione:
		```python
			s = input("Stringa: ")
			print(s[1:-1])
		```

- `[M2-B1-11]` Prezzo scontato
	- Testo: Leggi un prezzo in euro e una percentuale di sconto. Stampa il prezzo scontato e il risparmio.
	- Soluzione:
		```python
			prezzo = float(input("Prezzo (€): "))
			sconto = int(input("Sconto (%): "))
			risparmio = prezzo * sconto / 100
			prezzo_scontato = prezzo - risparmio
			print("Prezzo scontato:", prezzo_scontato)
			print("Risparmio:", risparmio)
		```

- `[M2-B2-01]` Positivo (solo if)
	- Testo: Leggi un numero intero e stampa "Positivo" solo se è maggiore di zero.
	- Soluzione:
		```python
			numero = int(input("Inserisci un numero: "))
			if numero > 0:
					print("Positivo")
		```

- `[M2-B2-02]` Parola lunga (solo if)
	- Testo: Leggi una parola e stampa "La parola è lunga" solo se ha più di 5 caratteri.
	- Soluzione:
		```python
			parola = input("Parola: ")
			if len(parola) > 5:
					print("La parola è lunga")
		```

- `[M2-B2-03]` Pari (solo if)
	- Testo: Leggi un numero intero e stampa "Pari" solo se è divisibile per 2.
	- Soluzione:
		```python
			n = int(input("Numero: "))
			if n % 2 == 0:
					print("Pari")
		```

- `[M2-B2-04]` Pari o Dispari
	- Testo: Leggi un numero intero e stampa "Pari" oppure "Dispari".
	- Soluzione:
		```python
			n = int(input("Numero: "))
			if n % 2 == 0:
					print("Pari")
			else:
					print("Dispari")
		```

- `[M2-B2-05]` Maggiore tra due numeri
	- Testo: Leggi due numeri interi e stampa il maggiore, o un messaggio se sono uguali.
	- Soluzione:
		```python
			a = int(input("Primo numero: "))
			b = int(input("Secondo numero: "))
			if a > b:
					print(a)
			elif b > a:
					print(b)
			else:
					print("I numeri sono uguali")
		```
	- Soluzione:
		```python
			a = int(input("Primo numero: "))
			b = int(input("Secondo numero: "))
			if a > b:
					print(a)
			elif a == b:
					print("I numeri sono uguali")
			else:
					print(b)
		```

- `[M2-B2-06]` Due nomi in ordine alfabetico
	- Testo: Leggi due nomi e stampali nell'ordine alfabetico corretto.
	- Soluzione:
		```python
			nome1 = input("Primo nome: ")
			nome2 = input("Secondo nome: ")
			if nome1 <= nome2:
					print(nome1)
					print(nome2)
			else:
					print(nome2)
					print(nome1)
		```

- `[M2-B2-07]` Vocale o consonante
	- Testo: Leggi una parola e controlla se inizia con una vocale; stampa un messaggio diverso nei due casi.
	- Soluzione:
		```python
			parola = input("Parola: ")
			if parola[0].lower() in "aeiou":
					print("Inizia con una vocale")
			else:
					print("Non inizia con una vocale")
		```

- `[M2-B2-08]` Negativo, Zero o Positivo
	- Testo: Leggi un numero intero e stampa "Negativo", "Zero" o "Positivo".
	- Soluzione:
		```python
			n = int(input("Numero: "))
			if n < 0:
					print("Negativo")
			elif n == 0:
					print("Zero")
			else:
					print("Positivo")
		```

- `[M2-B2-09]` Voto in parole
	- Testo: Leggi un voto da 0 a 30 e stampa: Insufficiente (< 18), Sufficiente (18–23), Buono (24–27), Ottimo (≥ 28).
	- Soluzione:
		```python
			voto = int(input("Voto: "))
			if voto < 18:
					print("Insufficiente")
			elif voto <= 23:
					print("Sufficiente")
			elif voto <= 27:
					print("Buono")
			else:
					print("Ottimo")
		```

- `[M2-B2-10]` Parola corta, media o lunga
	- Testo: Leggi una parola e stampa se è corta (< 4 lettere), media (4–7 lettere) o lunga (> 7 lettere).
	- Soluzione:
		```python
			parola = input("Parola: ")
			if len(parola) < 4:
					print("Corta")
			elif len(parola) <= 7:
					print("Media")
			else:
					print("Lunga")
		```

- `[M2-B2-11]` Mese successivo
	- Testo: Leggi un mese (1–12) e un anno e stampa il mese successivo con l'anno corretto (es. mese 12, anno 2024 → Gennaio 2025).
	- Soluzione:
		```python
			mese = int(input("Mese (1-12): "))
			anno = int(input("Anno: "))
			if mese == 12:
					mese_succ = 1
					anno_succ = anno + 1
			else:
					mese_succ = mese + 1
					anno_succ = anno
			if mese_succ == 1:
					nome_mese = "Gennaio"
			elif mese_succ == 2:
					nome_mese = "Febbraio"
			elif mese_succ == 3:
					nome_mese = "Marzo"
			elif mese_succ == 4:
					nome_mese = "Aprile"
			elif mese_succ == 5:
					nome_mese = "Maggio"
			elif mese_succ == 6:
					nome_mese = "Giugno"
			elif mese_succ == 7:
					nome_mese = "Luglio"
			elif mese_succ == 8:
					nome_mese = "Agosto"
			elif mese_succ == 9:
					nome_mese = "Settembre"
			elif mese_succ == 10:
					nome_mese = "Ottobre"
			elif mese_succ == 11:
					nome_mese = "Novembre"
			else:
					nome_mese = "Dicembre"
			print(nome_mese, anno_succ)
		```

- `[M2-B2-12]` Nato nel secolo scorso
	- Testo: Leggi nome, cognome e anno di nascita. Stampa sempre il nome e l'anno. Se l'anno è precedente al 2000, stampa anche "Nato/a nel secolo scorso".
	- Soluzione:
		```python
			nome = input("Nome: ")
			cognome = input("Cognome: ")
			anno = int(input("Anno di nascita: "))
			print(nome + " " + cognome)
			print(anno)
			if anno < 2000:
					print("Nato/a nel secolo scorso")
		```

- `[M2-B2-13]` Confronto con somma
	- Testo: Leggi due numeri interi. Stampa sempre entrambi i numeri. Se il primo è maggiore del secondo, stampa anche `Il primo è maggiore`. Stampa sempre anche la loro somma.
	- Soluzione:
		```python
			a = int(input("Primo numero: "))
			b = int(input("Secondo numero: "))
			print(a, b)
			if a > b:
					print("Il primo è maggiore")
			print("Somma:", a + b)
		```

- `[M2-B2-14]` Secondo carattere condizionale
	- Testo: Leggi una parola. Se ha più di 3 caratteri, stampa il secondo carattere; altrimenti stampa `Parola troppo corta`. In entrambi i casi stampa alla fine la lunghezza della parola.
	- Soluzione:
		```python
			parola = input("Parola: ")
			if len(parola) > 3:
					print(parola[1])
			else:
					print("Parola troppo corta")
			print(len(parola))
		```

- `[M2-B2-15]` Parola maiuscola o minuscola
	- Testo: Leggi una parola. Costruisci una variabile `risultato`: se la parola inizia con una lettera maiuscola, `risultato` vale `"maiuscola"`; altrimenti vale `"minuscola"`. Stampa `La parola è: ` seguito da `risultato`.
	- Soluzione:
		```python
			parola = input("Parola: ")
			if parola[0].isupper():
					risultato = "maiuscola"
			else:
					risultato = "minuscola"
			print("La parola è: " + risultato)
		```

- `[M2-B2-16]` Pari o dispari con variabile messaggio
	- Testo: Leggi un numero intero. Costruisci una variabile `messaggio`: se il numero è pari, `messaggio` vale `"pari"`; altrimenti vale `"dispari"`. Stampa `Il numero X è` seguito da `messaggio`.
	- Soluzione:
		```python
			n = int(input("Numero: "))
			if n % 2 == 0:
					messaggio = "pari"
			else:
					messaggio = "dispari"
			print("Il numero " + str(n) + " è " + messaggio)
		```

- `[M2-B2-17]` Ordina a e b
	- Testo: Leggi due interi a e b. Se a > b, scambia i valori. Stampa a e b in ordine crescente.
	- Soluzione:
		```python
			a = int(input("a: "))
			b = int(input("b: "))
			if a > b:
					tmp = a
					a = b
					b = tmp
			print(a, b)
		```

- `T1`
	- Codice:
		```python
			x = int(input("x: "))
			y = int(input("y: "))
			z = x + y
			if z > 10:
				x = x * 2
				etichetta = "grande"
			elif z > 0:
				x = x + 1
				etichetta = "medio"
			else:
				etichetta = "piccolo"
			print(etichetta + ": " + str(x))
		```
	- Testo: Traccia con input `3`, `4` e con input `6`, `7`.
	- Traccia: Input 3, 4
		| Passo | Istruzione | Memoria | Output |
		| --- | --- | --- | --- |
		| 1 | `x = int(input("x: "))` | `x → 3` | `x: ` |
		| 2 | `y = int(input("y: "))` | `x → 3, y → 4` | `y: ` |
		| 3 | `z = x + y` | `x → 3, y → 4, z → 7` | |
		| 4 | `if z > 10:` → FALSE | `x → 3, y → 4, z → 7` | |
		| 5 | `elif z > 0:` → TRUE | `x → 3, y → 4, z → 7` | |
		| 6 | `x = x + 1` | `x → 4, y → 4, z → 7` | |
		| 7 | `etichetta = "medio"` | `x → 4, y → 4, z → 7, etichetta → "medio"` | |
		| 8 | `print(etichetta + ": " + str(x))` | | `medio: 4` |
	- Traccia: Input 6, 7
		| Passo | Istruzione | Memoria | Output |
		| --- | --- | --- | --- |
		| 1 | `x = int(input("x: "))` | `x → 6` | `x: ` |
		| 2 | `y = int(input("y: "))` | `x → 6, y → 7` | `y: ` |
		| 3 | `z = x + y` | `x → 6, y → 7, z → 13` | |
		| 4 | `if z > 10:` → TRUE | `x → 6, y → 7, z → 13` | |
		| 5 | `x = x * 2` | `x → 12, y → 7, z → 13` | |
		| 6 | `etichetta = "grande"` | `x → 12, y → 7, z → 13, etichetta → "grande"` | |
		| 7 | `print(etichetta + ": " + str(x))` | | `grande: 12` |

- `T2`
	- Codice:
		```python
			s = input("Parola: ")
			n = len(s)
			if n > 4:
				s = s[0].upper() + s[1:]
				risultato = s + " (" + str(n) + ")"
			else:
				risultato = s.upper()
			print(risultato)
		```
	- Testo: Traccia con input `"python"` e con input `"ciao"`.
	- Traccia: Input "python"
		| Passo | Istruzione | Memoria | Output |
		| --- | --- | --- | --- |
		| 1 | `s = input("Parola: ")` | `s → "python"` | `Parola: ` |
		| 2 | `n = len(s)` | `s → "python", n → 6` | |
		| 3 | `if n > 4:` → TRUE | `s → "python", n → 6` | |
		| 4 | `s = s[0].upper() + s[1:]` | `s → "Python", n → 6` | |
		| 5 | `risultato = s + " (" + str(n) + ")"` | `s → "Python", n → 6, risultato → "Python (6)"` | |
		| 6 | `print(risultato)` | | `Python (6)` |
	- Traccia: Input "ciao"
		| Passo | Istruzione | Memoria | Output |
		| --- | --- | --- | --- |
		| 1 | `s = input("Parola: ")` | `s → "ciao"` | `Parola: ` |
		| 2 | `n = len(s)` | `s → "ciao", n → 4` | |
		| 3 | `if n > 4:` → FALSE | `s → "ciao", n → 4` | |
		| 4 | `risultato = s.upper()` | `s → "ciao", n → 4, risultato → "CIAO"` | |
		| 5 | `print(risultato)` | | `CIAO` |

- `T3`
	- Codice:
		```python
			a = int(input("a: "))
			b = int(input("b: "))
			if a > b:
				tmp = a
				a = b
				b = tmp
			diff = b - a
			if diff > 5:
				messaggio = "distanti"
			else:
				messaggio = "vicini"
			print(str(a) + " " + str(b) + " - " + messaggio)
		```
	- Testo: Traccia con input `9`, `2`.
	- Traccia: Input 9, 2
		| Passo | Istruzione | Memoria | Output |
		| --- | --- | --- | --- |
		| 1 | `a = int(input("a: "))` | `a → 9` | `a: ` |
		| 2 | `b = int(input("b: "))` | `a → 9, b → 2` | `b: ` |
		| 3 | `if a > b:` → TRUE | `a → 9, b → 2` | |
		| 4 | `tmp = a` | `a → 9, b → 2, tmp → 9` | |
		| 5 | `a = b` | `a → 2, b → 2, tmp → 9` | |
		| 6 | `b = tmp` | `a → 2, b → 9, tmp → 9` | |
		| 7 | `diff = b - a` | `a → 2, b → 9, tmp → 9, diff → 7` | |
		| 8 | `if diff > 5:` → TRUE | `a → 2, b → 9, tmp → 9, diff → 7` | |
		| 9 | `messaggio = "distanti"` | `a → 2, b → 9, tmp → 9, diff → 7, messaggio → "distanti"` | |
		| 10 | `print(str(a) + " " + str(b) + " - " + messaggio)` | | `2 9 - distanti` |

- `T4`
	- Codice:
		```python
			n = int(input("n: "))
			print("Inizio")
			if n < 0:
				print("Negativo")
				n = -n
				print("Valore assoluto: " + str(n))
			elif n == 0:
				print("Zero")
			else:
				print("Positivo")
				n = n * n
			print("Fine: " + str(n))
		```
	- Testo: Traccia con input `-3` e con input `4`.
	- Traccia: Input -3
		| Passo | Istruzione | Memoria | Output |
		| --- | --- | --- | --- |
		| 1 | `n = int(input("n: "))` | `n → -3` | `n: ` |
		| 2 | `print("Inizio")` | `n → -3` | `Inizio` |
		| 3 | `if n < 0:` → TRUE | `n → -3` | |
		| 4 | `print("Negativo")` | `n → -3` | `Negativo` |
		| 5 | `n = -n` | `n → 3` | |
		| 6 | `print("Valore assoluto: " + str(n))` | `n → 3` | `Valore assoluto: 3` |
		| 7 | `print("Fine: " + str(n))` | `n → 3` | `Fine: 3` |
	- Traccia: Input 4
		| Passo | Istruzione | Memoria | Output |
		| --- | --- | --- | --- |
		| 1 | `n = int(input("n: "))` | `n → 4` | `n: ` |
		| 2 | `print("Inizio")` | `n → 4` | `Inizio` |
		| 3 | `if n < 0:` → FALSE | `n → 4` | |
		| 4 | `elif n == 0:` → FALSE | `n → 4` | |
		| 5 | `print("Positivo")` | `n → 4` | `Positivo` |
		| 6 | `n = n * n` | `n → 16` | |
		| 7 | `print("Fine: " + str(n))` | `n → 16` | `Fine: 16` |

- `T5`
	- Codice:
		```python
			a = int(input("a: "))
			b = int(input("b: "))
			print("a=" + str(a) + " b=" + str(b))
			if a > b:
				print("a è maggiore")
				diff = a - b
				print("Differenza: " + str(diff))
			else:
				print("b è maggiore o uguale")
				diff = b - a
			print("Distanza: " + str(diff))
		```
	- Testo: Traccia con input `a = 7`, `b = 2` e con input `a = 3`, `b = 3`.
	- Traccia: Input a=7, b=2
		| Passo | Istruzione | Memoria | Output |
		| --- | --- | --- | --- |
		| 1 | `a = int(input("a: "))` | `a → 7` | `a: ` |
		| 2 | `b = int(input("b: "))` | `a → 7, b → 2` | `b: ` |
		| 3 | `print("a=" + str(a) + " b=" + str(b))` | `a → 7, b → 2` | `a=7 b=2` |
		| 4 | `if a > b:` → TRUE | `a → 7, b → 2` | |
		| 5 | `print("a è maggiore")` | `a → 7, b → 2` | `a è maggiore` |
		| 6 | `diff = a - b` | `a → 7, b → 2, diff → 5` | |
		| 7 | `print("Differenza: " + str(diff))` | `a → 7, b → 2, diff → 5` | `Differenza: 5` |
		| 8 | `print("Distanza: " + str(diff))` | `a → 7, b → 2, diff → 5` | `Distanza: 5` |
	- Traccia: Input a=3, b=3
		| Passo | Istruzione | Memoria | Output |
		| --- | --- | --- | --- |
		| 1 | `a = int(input("a: "))` | `a → 3` | `a: ` |
		| 2 | `b = int(input("b: "))` | `a → 3, b → 3` | `b: ` |
		| 3 | `print("a=" + str(a) + " b=" + str(b))` | `a → 3, b → 3` | `a=3 b=3` |
		| 4 | `if a > b:` → FALSE | `a → 3, b → 3` | |
		| 5 | `print("b è maggiore o uguale")` | `a → 3, b → 3` | `b è maggiore o uguale` |
		| 6 | `diff = b - a` | `a → 3, b → 3, diff → 0` | |
		| 7 | `print("Distanza: " + str(diff))` | `a → 3, b → 3, diff → 0` | `Distanza: 0` |

- `[M2-BOOL-01]` `x = 5`, `y = 3` → `x > 0 and y > 0`
	- Testo: Per ciascuna espressione e valori di variabili indicati, calcola il risultato (`True` o `False`) senza eseguire il codice.
	- Soluzione:
		```python
		True # entrambi True
		```

- `[M2-BOOL-02]` `x = -2`, `y = 4` → `x > 0 and y > 0`
	- Testo: Per ciascuna espressione e valori di variabili indicati, calcola il risultato (`True` o `False`) senza eseguire il codice.
	- Soluzione:
		```python
		False  # x non è > 0
		```

- `[M2-BOOL-03]` `x = -2`, `y = 4` → `x > 0 or y > 0`
	- Testo: Per ciascuna espressione e valori di variabili indicati, calcola il risultato (`True` o `False`) senza eseguire il codice.
	- Soluzione:
		```python
		True  # y > 0 è vero
		```

- `[M2-BOOL-04]` `x = -2`, `y = -1` → `x > 0 or y > 0`
	- Testo: Per ciascuna espressione e valori di variabili indicati, calcola il risultato (`True` o `False`) senza eseguire il codice.
	- Soluzione:
		```python
		False  # nessuno dei due è > 0
		```

- `[M2-BOOL-05]` `n = 7` → `not n > 10`
	- Testo: Per ciascuna espressione e valori di variabili indicati, calcola il risultato (`True` o `False`) senza eseguire il codice.
	- Soluzione:
		```python
		True  # 7 non è > 10, quindi not False = True
		```

- `[M2-BOOL-06]` `n = 15` → `not n > 10`
	- Testo: Per ciascuna espressione e valori di variabili indicati, calcola il risultato (`True` o `False`) senza eseguire il codice.
	- Soluzione:
		```python
		False  # 15 > 10 è True, quindi not True = False
		```

- `[M2-BOOL-07]` `eta = 20`, `ha_documento = False` → `eta >= 18 and ha_documento`
	- Testo: Per ciascuna espressione e valori di variabili indicati, calcola il risultato (`True` o `False`) senza eseguire il codice.
	- Soluzione:
		```python
		False  # ha_documento è False
		```

- `[M2-BOOL-08]` `eta = 16`, `ha_documento = True` → `eta >= 18 or ha_documento`
	- Testo: Per ciascuna espressione e valori di variabili indicati, calcola il risultato (`True` o `False`) senza eseguire il codice.
	- Soluzione:
		```python
		True  # ha_documento è True
		```

- `[M2-COMP-01]` Range 1–100
	- Testo: Scrivi un programma che legge un numero intero e stampa `Nel range` se è compreso tra 1 e 100 (estremi inclusi), `Fuori range` altrimenti.
	- Soluzione:
		```python
			n = int(input("Numero: "))
			if n >= 1 and n <= 100:
					print("Nel range")
			else:
					print("Fuori range")
		```

- `[M2-COMP-02]` Anno bisestile
	- Testo: Un anno è bisestile se è divisibile per 4 ma non per 100, oppure se è divisibile per 400. Scrivi un programma che verifica questa condizione.
	- Soluzione:
		```python
		anno = int(input("Anno: "))
		if (anno % 4 == 0 and anno % 100 != 0) or anno % 400 == 0:
				print("Bisestile")
		else:
				print("Non bisestile")
		```

- `[M2-COMP-03]` Stringhe vuote
	- Testo: Scrivi un programma che legge due stringhe e stampa `Almeno una è vuota` se almeno una delle due ha lunghezza zero, altrimenti stampa `Entrambe non vuote`.
	- Soluzione:
		```python
		s1 = input("Prima stringa: ")
		s2 = input("Seconda stringa: ")
		if len(s1) == 0 or len(s2) == 0:
				print("Almeno una è vuota")
		else:
				print("Entrambe non vuote")
		```

- `[M2-COMP-04]` Fuori range con not e and
	- Testo: Scrivi un programma che legge un numero intero e stampa `Fuori range` se è minore di 0 oppure maggiore di 10. Poi riscrivi la stessa condizione usando `not` e `and`.
	- Soluzione:
		```python
		n = int(input("Numero: "))
		if n < 0 or n > 10:
				print("Fuori range")
		```
	- Soluzione:
		```python
		# Versione equivalente con not e and (leggi di De Morgan)
		n = int(input("Numero: "))
		if not (n >= 0 and n <= 10):
				print("Fuori range")
		```

- `[M2-COMP-05]` Anni mancanti
	- Testo: Scrivi un programma che legge nome, età attuale ed età da raggiungere. Il programma stampa un saluto, la tua età attuale e tra quanti anni raggiungerai l'età desiderata.
	- Soluzione:
		```python
		nome = input("Nome: ")
		eta_attuale = int(input("Età attuale: "))
		eta_desiderata = int(input("Età desiderata: "))
		anni_mancanti = eta_desiderata - eta_attuale
		print("Ciao " + nome + ", oggi hai " + str(eta_attuale) + " anni, tra " + str(anni_mancanti) + " anni ne avrai " + str(eta_desiderata) + " come desideri")
		```


### 03 | Dall'if al while

- `E1` Ciao con età tra dieci anni
	- Codice:
		```python
		nome = input("Nome: ")
		cognome = input("Cognome: ")
		eta = input("Età: ")
		print("Ciao " + nome + " " + cognome + "!")
		print("Tra dieci anni avrai " + eta + 10 + " anni.")
		```
	- Testo: Esegui il codice, individua il tipo di errore e la riga in cui compare, spiega perché si verifica e correggi.
	- Soluzione:
		```python
		# TypeError alla riga: print("Tra dieci anni avrai " + eta + 10 + " anni.")
		# eta è una stringa (restituita da input()); non si può sommare una stringa a un intero.
		# Correzione: convertire eta a int e usare str() per la concatenazione.

		nome = input("Nome: ")
		cognome = input("Cognome: ")
		eta = int(input("Età: "))
		print("Ciao " + nome + " " + cognome + "!")
		print("Tra dieci anni avrai " + str(eta + 10) + " anni.")
		```

- `E2` Calcolo età dall'anno di nascita
	- Codice:
		```python
		anno_nascita = input("Anno di nascita: ")
		anno_corrente = 2026
		eta = anno_corrente - anno_nascita
		print("Hai circa " + str(eta) + " anni.")
		```
	- Testo: Esegui il codice, individua il tipo di errore e la riga in cui compare, spiega perché si verifica e correggi.
	- Soluzione:
		```python
		# TypeError alla riga: eta = anno_corrente - anno_nascita
		# anno_nascita è una stringa (restituita da input()); non si può sottrarre una stringa da un intero.
		# Correzione: convertire anno_nascita a int.

		anno_nascita = int(input("Anno di nascita: "))
		anno_corrente = 2026
		eta = anno_corrente - anno_nascita
		print("Hai circa " + str(eta) + " anni.")
		```

- `E3` Lunghezza di una parola
	- Codice:
		```python
		parola = input("Parola: ")
		lunghezza = len(parola)
		print("La parola ha " + lunghezza + " lettere.")
		```
	- Testo: Esegui il codice, individua il tipo di errore e la riga in cui compare, spiega perché si verifica e correggi.
	- Soluzione:
		```python
		# TypeError alla riga: print("La parola ha " + lunghezza + " lettere.")
		# lunghezza è un intero (restituito da len()); non si può concatenare un intero a una stringa.
		# Correzione: usare str() per convertire lunghezza in stringa.

		parola = input("Parola: ")
		lunghezza = len(parola)
		print("La parola ha " + str(lunghezza) + " lettere.")
		```

- `E4` Segno di un numero
	- Codice:
		```python
		x = int(input("Numero: "))
		if x > 0:
		    segno = "positivo"
		elif x < 0:
		    segno = "negativo"
		print("Il numero è " + segno)
		```
	- Testo: Esegui il codice, individua il tipo di errore e la riga in cui compare, spiega perché si verifica e correggi.
	- Soluzione:
		```python
		# NameError alla riga: print("Il numero è " + segno) quando x == 0.
		# Se x vale 0, né il ramo if né l'elif vengono eseguiti, e segno non viene mai definita.
		# Correzione: aggiungere un ramo else per coprire il caso x == 0.

		x = int(input("Numero: "))
		if x > 0:
		    segno = "positivo"
		elif x < 0:
		    segno = "negativo"
		else:
		    segno = "zero"
		print("Il numero è " + segno)
		```

- `[M3-TEST-01]` Parità — codice con bug
	- Codice:
		```python
		n = int(input("Numero: "))
		if n > 0 and n % 2 == 0:
		    print("pari")
		else:
		    print("dispari")
		```
	- Testo: Compila la tabella con l'output atteso e quello reale per ciascun input, poi spiega il bug e correggi il codice.
	- Traccia: Test
		| Input | Output atteso | Output reale |
		| --- | --- | --- |
		| `0` | `pari` | `dispari` |
		| `3` | `dispari` | `dispari` |
		| `-4` | `pari` | `dispari` |
		| `7` | `dispari` | `dispari` |
	- Soluzione:
		```python
		# Bug: la condizione n > 0 and n % 2 == 0 esclude 0 e i pari negativi (es. -4),
		# che vengono trattati come dispari perché n > 0 è False.
		# Correzione: basta controllare la divisibilità per 2, indipendentemente dal segno.

		n = int(input("Numero: "))
		if n % 2 == 0:
		    print("pari")
		else:
		    print("dispari")
		```

- `[M3-TEST-02]` Voto — codice con bug
	- Codice:
		```python
		voto = int(input("Voto: "))
		if voto >= 18:
		    print("promosso")

		if voto <= 18:
		    print("non promosso")
		```
	- Testo: Compila la tabella con l'output atteso e quello reale per ciascun input, poi spiega il bug e correggi il codice.
	- Traccia: Test
		| Input | Output atteso | Output reale |
		| --- | --- | --- |
		| `15` | `non promosso` | `non promosso` |
		| `30` | `promosso` | `promosso` |
		| `18` | `promosso` | `promosso` e `non promosso` (bug!) |
	- Soluzione:
		```python
		# Bug: i due if sono indipendenti, non alternativi.
		# Per voto = 18 entrambe le condizioni (>= 18 e <= 18) sono vere
		# e vengono stampate entrambe le stringhe.
		# Correzione: usare if-else per rendere i due rami mutualmente esclusivi.

		voto = int(input("Voto: "))
		if voto >= 18:
		    print("promosso")
		else:
		    print("non promosso")
		```

- `[M3-TEST-03]` Positivo o grande — codice con bug
	- Codice:
		```python
		x = int(input("x: "))
		if x > 0:
		    print("positivo")
		elif x > 10:
		    print("grande")
		else:
		    print("non positivo")
		```
	- Testo: Compila la tabella con l'output atteso e quello reale per ciascun input, poi spiega il bug e correggi il codice.
	- Traccia: Test
		| Input | Output atteso | Output reale |
		| --- | --- | --- |
		| `5` | `positivo` | `positivo` |
		| `15` | `grande` | `positivo` |
		| `-2` | `non positivo` | `non positivo` |
	- Soluzione:
		```python
		# Bug: l'elif x > 10 non viene mai raggiunto per numeri > 10,
		# perché la condizione x > 0 è già vera e Python si ferma al primo ramo vero.
		# Correzione: controllare prima il caso più restrittivo (> 10).

		x = int(input("x: "))
		if x > 10:
		    print("grande")
		elif x > 0:
		    print("positivo")
		else:
		    print("non positivo")
		```

- `[M3-TEST-04]` FizzBuzz
	- Testo: Scrivi e testa il programma FizzBuzz. Il caso divisibile per entrambi va controllato per primo.
	- Soluzione:
		```python
		n = int(input("Numero: "))
		if n % 3 == 0 and n % 5 == 0:
				print("fizzbuzz")
		elif n % 3 == 0:
				print("fizz")
		elif n % 5 == 0:
				print("buzz")
		else:
				print(n)
		```

- `[M3-WHILE-01]` Numeri da 1 a 10
	- Testo: Scrivi un programma che stampa i numeri da 1 a 10.
	- Soluzione:
		```python
		i = 1
		while i <= 10:
				print(i)
				i = i + 1
		```

- `[M3-WHILE-02]` Numeri da n a 0
	- Testo: Leggi un numero n e stampa i numeri da n a 0.
	- Soluzione:
		```python
		n = int(input("n: "))
		while n >= 0:
				print(n)
				n = n - 1
		```

- `[M3-WHILE-03]` Numeri da -n a n
	- Testo: Leggi un numero n e stampa i numeri da -n a n.
	- Soluzione:
		```python
		n = int(input("n: "))
		i = -n
		while i <= n:
				print(i)
				i = i + 1
		```

- `[M3-WHILE-04]` Lettura delle pagine
	- Testo: Dato pagine = 120, stampa "Sto leggendo la pagina 1", "Sto leggendo la pagina 2", … fino all'ultima.
	- Soluzione:
		```python
		pagine = 120
		i = 1
		while i <= pagine:
				print("Sto leggendo la pagina", i)
				i = i + 1
		```

- `[M3-WHILE-05]` Numeri dispari tra 1 e 10
	- Testo: Stampa i numeri dispari compresi tra 1 e 10 (inclusi).
	- Soluzione:
		```python
		i = 1
		while i <= 10:
				print(i)
				i = i + 2
		```

- `[M3-WHILE-06]` Lettere in posizione pari
	- Testo: Leggi una parola e stampa solo le lettere in posizione pari (indice 0, 2, 4, …).
	- Soluzione:
		```python
		parola = input("Parola: ")
		i = 0
		while i < len(parola):
				print(parola[i])
				i = i + 2
		```

- `[M3-WHILE-07]` Potenze fino a 1000
	- Testo: Leggi un intero e stampa le potenze di quel numero finché il valore non eccede 1000 (es. 2^1=2, 2^2=4, …, 2^9=512).
	- Soluzione:
		```python
		base = int(input("Base: "))
		esp = 1
		while base ** esp <= 1000:
				print(str(base) + "^" + str(esp) + " = " + str(base ** esp))
				esp = esp + 1
		```

- `[M3-WHILE-08]` Input ripetuto fino a "fine"
	- Testo: Chiedi parole all'utente e continua finché non scrive "fine".
	- Soluzione:
		```python
		parola = input("Inserisci una parola: ")
		while parola != "fine":
				parola = input("Inserisci una parola: ")
		```

### P1 | Prova intermedia 1 | prova=true

- Sim. 1 — Es. 13 — `[EX-BASE-13]` Ordinamento di due date di nascita
	- Testo: Leggi due date nel formato GG-MM-AAAA e stampale in ordine cronologico (dalla più antica alla più recente). Usa slicing per estrarre anno, mese e giorno.
	- Soluzione:
		```python
		data1 = input("Prima data (GG-MM-AAAA): ")
		giorno1 = data1[0:2]
		mese1 = data1[3:5]
		anno1 = data1[6:10]
		data2 = input("Seconda data (GG-MM-AAAA): ")
		giorno2 = data2[0:2]
		mese2 = data2[3:5]
		anno2 = data2[6:10]
		if anno1 < anno2:
				print(data1)
				print(data2)
		elif anno1 == anno2:
				if mese1 < mese2:
						print(data1)
						print(data2)
				elif mese1 == mese2:
						if giorno1 <= giorno2:
								print(data1)
								print(data2)
						else:
								print(data2)
								print(data1)
				else:
						print(data2)
						print(data1)
		else:
				print(data2)
				print(data1)
		```
	- Soluzione:
		```python
		# Riorganizza ogni data in AAAA-MM-GG per poterle confrontare direttamente come stringhe
		data1 = input("Prima data (GG-MM-AAAA): ")
		data2 = input("Seconda data (GG-MM-AAAA): ")
		if data1[6:10] + data1[3:5] + data1[0:2] <= data2[6:10] + data2[3:5] + data2[0:2]:
				print(data1)
				print(data2)
		else:
				print(data2)
				print(data1)
		```

- Sim. 1 — Es. 14 — `[EX-BASE-14]` Controllo intervallo
	- Testo: Leggi tre numeri: l'estremo sinistro, il destro e un terzo numero. Stampa "Nel range" o "Fuori range".
	- Soluzione:
		```python
		a = int(input("Estremo sinistro: "))
		b = int(input("Estremo destro: "))
		n = int(input("Numero da controllare: "))
		if a <= n <= b:
				print("Nel range")
		else:
				print("Fuori range")
		```

- Sim. 1 — Es. 15 — `[EX-BASE-15]` Numeri pari in un intervallo
	- Testo: Leggi due numeri a e b e stampa tutti i numeri pari compresi tra a e b (estremi inclusi).
	- Soluzione:
		```python
		a = int(input("Primo numero: "))
		b = int(input("Secondo numero: "))
		i = a
		while i <= b:
				if i % 2 == 0:
						print(i)
				i = i + 1
		```
	- Soluzione:
		```python
		a = int(input("Primo numero: "))
		b = int(input("Secondo numero: "))
		if a % 2 == 1:
			a = a + 1
		while a <= b:
				print(a)
				a = a + 2
		```

- Sim. 1 — Es. 16 — `[EX-BASE-16]` Somma finché supera 50
	- Testo: Leggi numeri dall'input finché la somma non supera 50. Stampa la somma finale.
	- Soluzione:
		```python
		somma = 0
		while somma <= 50:
				n = int(input("Inserisci un numero: "))
				somma = somma + n
		print(somma)
		```

- Sim. 1 — Es. 17 — `[EX-BASE-17]` Sequenza con sentinella 0: contatore e somma
	- Testo: Leggi interi fino a 0. Stampa quanti valori sono stati inseriti (escluso lo zero) e la loro somma.
	- Soluzione:
		```python
		contatore = 0
		somma = 0
		n = int(input("Inserisci un numero: "))
		while n != 0:
				contatore = contatore + 1
				somma = somma + n
				n = int(input("Inserisci un numero: "))
		print("Numero di valori:", contatore)
		print("Somma:", somma)
		```

- Sim. 2 — Es. 7 — `[EX-SIM2-07]` FizzBuzz (da 1 a n)
	- Testo: Leggi un intero positivo n e stampa tutti i numeri da 1 a n, sostituendo: multipli di 3 → "fizz", multipli di 5 → "buzz", multipli di entrambi → "fizzbuzz".
	- Soluzione:
		```python
		n = int(input("Inserisci un numero: "))
		i = 1
		while i <= n:
				if i % 3 == 0 and i % 5 == 0:
						print("fizzbuzz")
				elif i % 3 == 0:
						print("fizz")
				elif i % 5 == 0:
						print("buzz")
				else:
						print(i)
				i = i + 1
		```
	- Video: https://youtu.be/IxA4z9hMk98

- Sim. 2 — Es. 8 — `[EX-SIM2-08]` Conta positivi e negativi con sentinella 0
	- Testo: Leggi interi fino a 0. Stampa quanti erano positivi e quanti negativi (lo zero non viene contato).
	- Soluzione:
		```python
		positivi = 0
		negativi = 0
		n = int(input("Inserisci un numero: "))
		while n != 0:
				if n > 0:
						positivi = positivi + 1
				else:
						negativi = negativi + 1
				n = int(input("Inserisci un numero: "))
		print("Positivi:", positivi)
		print("Negativi:", negativi)
		```
	- Video: https://youtu.be/iHr-kacqp60

- Compitino — Es. 7A — `[COMP1-G7-01]` Posizioni multiple di 3
	- Testo: Scrivi un programma che prende in input una stringa e stampa solo le lettere in posizioni multiple di 3 (0, 3, 6, …). Es.: "soqquadro" → s, q, d.
	- Soluzione:
		```python
		s = input("Inserisci una parola: ")
		i = 0
		while i < len(s):
				if i % 3 == 0:
						print(s[i])
				i = i + 1
		```
	- Soluzione:
		```python
		# Versione alternativa: avanza di 3 direttamente
		s = input("Inserisci una parola: ")
		i = 0
		while i < len(s):
				print(s[i])
				i = i + 3
		```
	- Video: https://youtu.be/iyx-HfJ8zjg

- Compitino — Es. 7B — `[COMP1-G7-02]` Posizioni non multiple di 3
	- Testo: Scrivi un programma che stampa solo le lettere in posizioni che non sono multiple di 3. Es.: "soqquadro" → o, q, u, a, r, o.
	- Soluzione:
		```python
		s = input("Inserisci una parola: ")
		i = 0
		while i < len(s):
				if i % 3 != 0:
						print(s[i])
				i = i + 1
		```
	- Video: https://youtu.be/mZSUgWUqyDY

- Compitino — Es. 8A — `[COMP1-G8-01]` Divisione intera senza `//`
	- Testo: Leggi due interi a e b. Se a > b, calcola a // b senza usare //. Altrimenti stampa un messaggio di errore. Es.: 20, 3 → 6; 10, 100 → errore.
	- Soluzione:
		```python
		a = int(input())
		b = int(input())
		if a > b:
				quoziente = 0
				resto = a
				while resto >= b:
						resto = resto - b
						quoziente = quoziente + 1
				print(quoziente)
		else:
				print("Attenzione, a è minore di b!")
		```
	- Video: https://youtu.be/N5I84wrZCfY

- Compitino — Es. 8B — `[COMP1-G8-02]` Potenza senza `**`
	- Testo: Leggi due interi a e b. Se a < b, calcola a ** b senza usare **. Altrimenti stampa un messaggio di errore. Es.: 2, 3 → 8; 3, 2 → errore.
	- Soluzione:
		```python
		a = int(input())
		b = int(input())
		if a < b:
				risultato = 1
				i = 0
				while i < b:
						risultato = risultato * a
						i = i + 1
				print(risultato)
		else:
				print("Attenzione, a è maggiore di b!")
		```
	- Video: https://www.youtube.com/watch?v=eg7-XmZBmQU

### 04 | Iterazione con sentinelle, e liste

- `[M4-WB-01]` Prodotto con sentinella 0
	- Testo: Chiedi numeri all'utente. Fermati quando inserisce 0. Stampa il prodotto di tutti i numeri inseriti. (Es.: 2 3 4 0 → 24)
	- Soluzione:
		```python
		prodotto = 1
		n = int(input("Numero: "))
		while n != 0:
		    prodotto = prodotto * n
		    n = int(input("Numero: "))
		print(prodotto)
		```

- `[M4-WB-02]` Pari/dispari con sentinella 0
	- Testo: Chiedi numeri all'utente fino a 0. Per ogni numero inserito stampa "pari" o "dispari". (Es.: 3 4 7 0 → dispari, pari, dispari)
	- Soluzione:
		```python
		n = int(input("Numero: "))
		while n != 0:
		    if n % 2 == 0:
		        print("pari")
		    else:
		        print("dispari")
		    n = int(input("Numero: "))
		```

- `[M4-WB-03]` Fattoriale
	- Testo: Leggi un intero positivo n e stampa n! = 1 × 2 × … × n. (Es.: 5 → 120)
	- Soluzione:
		```python
		n = int(input("n: "))
		fattoriale = 1
		i = 1
		while i <= n:
		    fattoriale = fattoriale * i
		    i = i + 1
		print(fattoriale)
		```

- `[M4-WB-04]` Conta vocali
	- Testo: Chiedi una stringa e calcola quante vocali contiene. (Es.: informatica → 5)
	- Soluzione:
		```python
		s = input("Stringa: ")
		contatore = 0
		i = 0
		while i < len(s):
		    if s[i] in "aeiouAEIOU":
		        contatore = contatore + 1
		    i = i + 1
		print(contatore)
		```

- `[M4-WB-05]` Somma fino a 100
	- Testo: Chiedi numeri interi e sommali. Fermati quando la somma raggiunge o supera 100. Stampa la somma. (Es.: 30 25 20 40 → 115)
	- Soluzione:
		```python
		somma = 0
		while somma < 100:
		    n = int(input("Numero: "))
		    somma = somma + n
		print(somma)
		```

- `[M4-WB-06]` Parole fino a 20 caratteri totali
	- Testo: Chiedi parole una alla volta. Fermati quando la lunghezza totale supera 20 caratteri. Stampa tutte le parole lette separate da spazio. (Es.: casa libro sole mare notte → "casa libro sole mare notte")
	- Soluzione:
		```python
		parola = input("Parola: ")
		totale = len(parola)
		risultato = parola
		while totale <= 20:
		    parola = input("Parola: ")
		    totale = totale + len(parola)
		    risultato = risultato + " " + parola
		print(risultato)
		```

- `[M4-WB-07]` Quante divisioni per 2
	- Testo: Leggi un intero positivo n. Conta quante volte si può dividere per 2 prima che diventi dispari. (Es.: 24 → 3, perché 24→12→6→3)
	- Soluzione:
		```python
		n = int(input("n: "))
		contatore = 0
		while n % 2 == 0:
		    n = n // 2
		    contatore = contatore + 1
		print(contatore)
		```

- `[M4-WB-08]` Caratteri prima della prima vocale
	- Testo: Leggi una stringa e stampa tutti i caratteri che precedono la prima vocale. Se non ci sono vocali, stampa tutta la stringa. (Es.: stringa → str; bcdf → bcdf)
	- Soluzione:
		```python
		s = input("Stringa: ")
		risultato = ""
		i = 0
		while i < len(s) and s[i] not in "aeiouAEIOU":
		    risultato = risultato + s[i]
		    i = i + 1
		print(risultato)
		```
	- Soluzione:
		```python
		s = input("Stringa: ")
		posizione_vocale = 0
		while posizione_vocale < len(s) and s[posizione_vocale] not in "aeiouAEIOU":
		    posizione_vocale = posizione_vocale + 1
		print(s[:posizione_vocale])
		```

- `[M4-WB-09]` Validazione anno 1900–2100
	- Testo: Chiedi un anno compreso tra 1900 e 2100. Ripeti la richiesta finché l'anno non è valido. Stampa "Anno accettato: X". (Es.: 1800 2050 → Anno accettato: 2050)
	- Soluzione:
		```python
		anno = int(input("Anno: "))
		while anno < 1900 or anno > 2100:
		    anno = int(input("Anno: "))
		print("Anno accettato: " + str(anno))
		```

- `[M4-WB-10]` Validazione parola con maiuscola
	- Testo: Chiedi una parola. Ripeti finché non inizia con una lettera maiuscola. Stampa "Parola accettata". (Es.: ciao Ciao → Parola accettata)
	- Soluzione:
		```python
		parola = input("Parola: ")
		while not parola[0].isupper():
		    parola = input("Parola: ")
		print("Parola accettata")
		```

- `[M4-WB-11]` Parola lunga o breve con sentinella "fine"
	- Testo: Chiedi parole fino a "fine". Per ogni parola, stampa "parola lunga" se ha più di 5 caratteri, "parola breve" altrimenti. (Es.: gatto programmazione sole fine → breve, lunga, breve)
	- Soluzione:
		```python
		parola = input("Parola: ")
		while parola != "fine":
		    if len(parola) > 5:
		        print("parola lunga")
		    else:
		        print("parola breve")
		    parola = input("Parola: ")
		```

- `[M4-WB-12]` Cerca "informatica"
	- Testo: Chiedi parole fino a "fine". Stampa "sì" se tra le parole compare "informatica", "no" altrimenti.
	- Soluzione:
		```python
		trovata = False
		parola = input("Parola: ")
		while parola != "fine":
		    if parola == "informatica":
		        trovata = True
		    parola = input("Parola: ")
		if trovata:
		    print("sì")
		else:
		    print("no")
		```

- `[M4-WB-13]` Multipli di 3 fino a n
	- Testo: Leggi un intero positivo n e stampa tutti i multipli di 3 da 3 fino a n. (Es.: 15 → 3 6 9 12 15)
	- Soluzione:
		```python
		n = int(input("n: "))
		i = 3
		while i <= n:
		    print(i)
		    i = i + 3
		```

- `[M4-WB-14]` Quanti numeri prima che il prodotto superi 1000
	- Testo: Chiedi numeri interi. Fermati quando il prodotto supera 1000. Stampa quanti numeri hai letto. (Es.: 4 5 6 9 → 4)
	- Soluzione:
		```python
		prodotto = 1
		contatore = 0
		while prodotto <= 1000:
		    n = int(input("Numero: "))
		    prodotto = prodotto * n
		    contatore = contatore + 1
		print(contatore)
		```

- `[M4-WB-15]` Divisioni per 3 ripetute
	- Testo: Leggi un intero positivo n. Finché n è divisibile per 3, dividilo per 3. Stampa il valore finale. (Es.: 27 → 1; 12 → 4)
	- Soluzione:
		```python
		n = int(input("n: "))
		while n % 3 == 0:
		    n = n // 3
		print(n)
		```

- `[M4-WB-16]` Validazione numero dispari
	- Testo: Chiedi un numero intero dispari. Ripeti finché non inserisce un dispari. Stampa "Numero dispari accettato". (Es.: 4 6 7 → Numero dispari accettato)
	- Soluzione:
		```python
		n = int(input("Numero: "))
		while n % 2 == 0:
		    n = int(input("Numero: "))
		print("Numero dispari accettato")
		```

- `[M4-WI-01]` Tabellina con validazione
	- Testo: Leggi un numero tra 1 e 10 (con validazione) e stampa la sua tabellina (da ×0 a ×10).
	- Soluzione:
		```python
		n = int(input("Numero (1-10): "))
		while n < 1 or n > 10:
		    n = int(input("Numero (1-10): "))
		i = 0
		while i <= 10:
		    print(str(n) + " x " + str(i) + " = " + str(n * i))
		    i = i + 1
		```

- `[M4-WI-02]` Conta vocali totali
	- Testo: Chiedi parole fino a "fine". Per ogni parola calcola quante vocali ha. Stampa il numero totale di vocali.
	- Soluzione:
		```python
		totale = 0
		parola = input("Parola: ")
		while parola != "fine":
		    i = 0
		    while i < len(parola):
		        if parola[i] in "aeiouAEIOU":
		            totale = totale + 1
		        i = i + 1
		    parola = input("Parola: ")
		print(totale)
		```

- `[M4-WI-03]` Parole che finiscono per vocale
	- Testo: Chiedi parole fino a "fine". Stampa quante delle parole inserite finiscono per vocale.
	- Soluzione:
		```python
		contatore = 0
		parola = input("Parola: ")
		while parola != "fine":
		    if parola[-1] in "aeiouAEIOU":
		        contatore = contatore + 1
		    parola = input("Parola: ")
		print(contatore)
		```

- `[M4-WI-04]` Parole con iniziale maiuscola
	- Testo: Leggi parole fino a "fine". Conta quante iniziano con una lettera maiuscola.
	- Soluzione:
		```python
		contatore = 0
		parola = input("Parola: ")
		while parola != "fine":
		    if parola[0].isupper():
		        contatore = contatore + 1
		    parola = input("Parola: ")
		print(contatore)
		```

- `[M4-WI-05]` Massimo tra numeri inseriti
	- Testo: Leggi interi fino a 0. Stampa il massimo. Se non viene inserito nessun numero prima dello zero, stampa un messaggio apposito.
	- Soluzione:
		```python
		n = int(input("Numero: "))
		if n == 0:
		    print("nessun numero inserito")
		else:
		    massimo = n
		    n = int(input("Numero: "))
		    while n != 0:
		        if n > massimo:
		            massimo = n
		        n = int(input("Numero: "))
		    print(massimo)
		```

- `[M4-WI-06]` Pari e dispari
	- Testo: Leggi interi fino a 0. Stampa quanti erano pari e quanti dispari, e quale categoria era più numerosa.
	- Soluzione:
		```python
		pari = 0
		dispari = 0
		n = int(input("Numero: "))
		while n != 0:
		    if n % 2 == 0:
		        pari = pari + 1
		    else:
		        dispari = dispari + 1
		    n = int(input("Numero: "))
		print("Pari:", pari)
		print("Dispari:", dispari)
		if pari > dispari:
		    print("Più pari")
		elif dispari > pari:
		    print("Più dispari")
		else:
		    print("Uguali")
		```

- `[M4-WI-07]` Media con sentinella 0
	- Testo: Leggi interi fino a 0. Calcola la media. Se non viene inserito nessun valore, stampa "nessun dato".
	- Soluzione:
		```python
		somma = 0
		contatore = 0
		n = int(input("Numero: "))
		while n != 0:
		    somma = somma + n
		    contatore = contatore + 1
		    n = int(input("Numero: "))
		if contatore == 0:
		    print("nessun dato")
		else:
		    print(somma / contatore)
		```

- `[M4-WI-08]` Combinazioni pagine × righe
	- Testo: Dati pagine = 3 e righe = 5, stampa tutte le combinazioni (Pagina 1 Riga 1, …, Pagina 3 Riga 5).
	- Soluzione:
		```python
		pagine = 3
		righe = 5
		p = 1
		while p <= pagine:
		    r = 1
		    while r <= righe:
		        print("Pagina", p, "Riga", r)
		        r = r + 1
		    p = p + 1
		```

- `[M4-WI-09]` Quadrato di asterischi
	- Testo: Leggi n e stampa un quadrato di asterischi n × n.
	- Soluzione:
		```python
		n = int(input("n: "))
		r = 0
		while r < n:
		    c = 0
		    riga = ""
		    while c < n:
		        riga = riga + "*"
		        c = c + 1
		    print(riga)
		    r = r + 1
		```

- `[M4-WI-10]` Sequenza di Collatz
	- Testo: Leggi un intero positivo n. Se pari: n // 2; se dispari: 3*n+1. Ripeti fino a 1. Stampa ogni valore e quanti passi sono stati necessari.
	- Soluzione:
		```python
		n = int(input("n: "))
		passi = 0
		while n != 1:
		    print(n)
		    if n % 2 == 0:
		        n = n // 2
		    else:
		        n = 3 * n + 1
		    passi = passi + 1
		print(n)
		print("Passi:", passi)
		```

- `[M4-WI-11]` Sequenza crescente
	- Testo: Leggi interi uno alla volta. Continua a leggere finché ogni nuovo numero è strettamente maggiore del precedente. Quando la sequenza si interrompe (o l'utente inserisce 0), stampa quanti numeri validi hai letto.
	- Soluzione:
		```python
		n = int(input("Numero: "))
		if n == 0:
		    print(0)
		else:
		    contatore = 1
		    precedente = n
		    n = int(input("Numero: "))
		    while n != 0 and n > precedente:
		        contatore = contatore + 1
		        precedente = n
		        n = int(input("Numero: "))
		    print(contatore)
		```

- `[M4-WI-12]` Sequenza crescente con buchi
	- Testo: Leggi interi positivi uno alla volta, finché ogni nuovo numero è strettamente maggiore del precedente. Per ogni coppia consecutiva (a, b) con b > a+1, stampa anche tutti i numeri interi compresi tra a e b (esclusi). Es.: se l'utente inserisce 13 poi 16, stampa anche 14 e 15.
	- Soluzione:
		```python
		precedente = int(input("Numero: "))
		n = int(input("Numero: "))
		while n > precedente:
		    if n > precedente + 1:
		        k = precedente + 1
		        while k < n:
		            print(k)
		            k = k + 1
		    precedente = n
		    n = int(input("Numero: "))
		```

- `[M4-WI-13]` Primi n numeri di Fibonacci
	- Testo: Leggi un intero positivo n e stampa i primi n numeri della sequenza di Fibonacci: 0, 1, 1, 2, 3, 5, 8, …
	- Soluzione:
		```python
		n = int(input("n: "))
		a = 0
		b = 1
		i = 0
		while i < n:
		    print(a)
		    c = a + b
		    a = b
		    b = c
		    i = i + 1
		```

- `[M4-WA-01]` Prima parola lunga o "fine"
	- Testo: Leggi parole fino alla prima più lunga di 6 caratteri o fino a "fine". Se trovi una parola lunga, stampala; altrimenti stampa "nessuna".
	- Soluzione:
		```python
		parola = input("Parola: ")
		trovata = False
		while parola != "fine" and not trovata:
		    if len(parola) > 6:
		        trovata = True
		    else:
		        parola = input("Parola: ")
		if trovata:
		    print(parola)
		else:
		    print("nessuna")
		```

- `[M4-WA-02]` Tavola pitagorica 1–10
	- Testo: Stampa la tavola pitagorica da 1 a 10 (1×1=1, …, 10×10=100).
	- Soluzione:
		```python
		i = 1
		while i <= 10:
		    j = 1
		    while j <= 10:
		        print(str(i) + "x" + str(j) + "=" + str(i * j))
		        j = j + 1
		    i = i + 1
		```

- `[M4-WA-03]` Cifre di un numero
	- Testo: Leggi un intero positivo n e stampa tutte le sue cifre una per riga, dalla meno significativa (es. 374 → 4, 7, 3). Senza usare stringhe: usa divisione e modulo.
	- Soluzione:
		```python
		n = int(input("n: "))
		while n > 0:
		    print(n % 10)
		    n = n // 10
		```

- `[M4-WA-04]` Scarto rispetto al precedente
	- Testo: Leggi una sequenza di numeri positivi terminata da -1. Stampa ogni numero con il suo scarto rispetto al precedente (es. +3, -5). Per il primo numero lo scarto non esiste.
	- Soluzione:
		```python
		n = int(input("Numero: "))
		primo = True
		precedente = 0
		while n != -1:
		    if primo:
		        print(str(n) + " (nessuno scarto)")
		        primo = False
		    else:
		        scarto = n - precedente
		        if scarto >= 0:
		            print(str(n) + " (+" + str(scarto) + ")")
		        else:
		            print(str(n) + " (" + str(scarto) + ")")
		    precedente = n
		    n = int(input("Numero: "))
		```

- `[M4-WA-05]` Sequenza non-decrescente?
	- Testo: Leggi interi fino a 0. Stampa "sì" se la sequenza è non-decrescente (ogni numero ≥ al precedente), "no" altrimenti.
	- Soluzione:
		```python
		n = int(input("Numero: "))
		crescente = True
		precedente = n
		n = int(input("Numero: "))
		while n != 0:
			if n < precedente:
				crescente = False
			precedente = n
			n = int(input("Numero: "))
		if crescente:
			print("sì")
		else:
			print("no")
		```

- `[M4-WA-06]` Numero primo
	- Testo: Leggi un intero positivo n e stabilisci se è primo (divisibile solo per 1 e per se stesso).
	- Soluzione:
		```python
		n = int(input("n: "))
		if n < 2:
		    print("no")
		else:
		    i = 2
		    primo = True
		    while i < n and primo:
		        if n % i == 0:
		            primo = False
		        i = i + 1
		    if primo:
		        print("sì")
		    else:
		        print("no")
		```

- `[M4-WA-07]` Ogni 1 è seguito da 2?
	- Testo: Leggi interi fino a 0. Controlla se ogni 1 è seguito da un 2. Es.: 9 1 2 6 1 2 0 → "Sequenza corretta"; 9 1 6 1 2 0 → "Sequenza errata".
	- Soluzione:
		```python
		numeri = []
		n = int(input("Numero: "))
		while n != 0:
		    numeri.append(n)
		    n = int(input("Numero: "))

		corretto = True
		i = 0
		while i < len(numeri) - 1:
		    if numeri[i] == 1 and numeri[i + 1] != 2:
		        corretto = False
		    i = i + 1
		if len(numeri) > 0 and numeri[-1] == 1:
		    corretto = False

		if corretto:
		    print("Sequenza corretta")
		else:
		    print("Sequenza errata")
		```

- `[M4-LB-01]` Lista da 1 a 5 con append
	- Testo: Crea una lista con i numeri da 1 a 5 usando append in un ciclo while. Poi stampa la lista.
	- Soluzione:
		```python
		lista = []
		i = 1
		while i <= 5:
		    lista.append(i)
		    i = i + 1
		print(lista)
		```

- `[M4-LB-02]` Primo e ultimo voto
	- Testo: Data la lista voti = [24, 28, 30, 18, 27], stampa il primo e l'ultimo voto.
	- Soluzione:
		```python
		voti = [24, 28, 30, 18, 27]
		print(voti[0])
		print(voti[-1])
		```

- `[M4-LB-03]` Secondo e penultimo nome
	- Testo: Data la lista nomi = ["Alice", "Bruno", "Carla", "Dario"], stampa il secondo e il penultimo nome.
	- Soluzione:
		```python
		nomi = ["Alice", "Bruno", "Carla", "Dario"]
		print(nomi[1])
		print(nomi[-2])
		```

- `[M4-LB-04]` Leggi numeri in lista
	- Testo: Leggi numeri interi dall'input fino a 0 e salvali in una lista. Stampa la lista.
	- Soluzione:
		```python
		numeri = []
		n = int(input("Numero: "))
		while n != 0:
		    numeri.append(n)
		    n = int(input("Numero: "))
		print(numeri)
		```

- `[M4-LB-05]` Leggi parole in lista
	- Testo: Leggi parole dall'input fino a "fine" e salvale in una lista. Stampa quante parole sono state inserite.
	- Soluzione:
		```python
		parole = []
		parola = input("Parola: ")
		while parola != "fine":
		    parole.append(parola)
		    parola = input("Parola: ")
		print(len(parole))
		```

- `[M4-LB-06]` Somma degli elementi
	- Testo: Data una lista di numeri, calcola e stampa la somma di tutti gli elementi senza usare sum().
	- Soluzione:
		```python
		numeri = [3, 1, 4, 1, 5, 9, 2, 6]
		somma = 0
		i = 0
		while i < len(numeri):
		    somma = somma + numeri[i]
		    i = i + 1
		print(somma)
		```

- `[M4-LB-07]` Numeri maggiori di 10
	- Testo: Data una lista di numeri, stampa solo quelli maggiori di 10.
	- Soluzione:
		```python
		numeri = [5, 12, 3, 17, 8, 21, 4]
		i = 0
		while i < len(numeri):
		    if numeri[i] > 10:
		        print(numeri[i])
		    i = i + 1
		```

- `[M4-LB-08]` Parole che iniziano per vocale
	- Testo: Data una lista di parole, stampa solo quelle che iniziano per vocale.
	- Soluzione:
		```python
		parole = ["alice", "bruno", "elena", "ciao", "ugo"]
		i = 0
		while i < len(parole):
		    if parole[i][0] in "aeiouAEIOU":
		        print(parole[i])
		    i = i + 1
		```

- `[M4-LB-09]` Prezzo massimo senza max()
	- Testo: Data la lista prezzi = [3.5, 1.2, 7.8, 4.0], stampa il prezzo più alto scorrendo la lista con while e tenendo traccia del massimo.
	- Soluzione:
		```python
		prezzi = [3.5, 1.2, 7.8, 4.0]
		massimo = prezzi[0]
		i = 1
		while i < len(prezzi):
		    if prezzi[i] > massimo:
		        massimo = prezzi[i]
		    i = i + 1
		print(massimo)
		```

- `[M4-LB-10]` Parole in ordine inverso
	- Testo: Leggi parole finché l'utente non scrive "fine" e stampale in ordine inverso rispetto all'inserimento.
	- Soluzione:
		```python
		parole = []
		parola = input("Parola: ")
		while parola != "fine":
		    parole.append(parola)
		    parola = input("Parola: ")
		i = len(parole) - 1
		while i >= 0:
		    print(parole[i])
		    i = i - 1
		```

- `[M4-LI-01]` Lista dei soli pari
	- Testo: Leggi interi fino a 0. Costruisci una lista con i soli numeri pari e stampala. (Es.: 3 8 5 4 7 2 0 → [8, 4, 2])
	- Soluzione:
		```python
		pari = []
		n = int(input("Numero: "))
		while n != 0:
		    if n % 2 == 0:
		        pari.append(n)
		    n = int(input("Numero: "))
		print(pari)
		```
	- Video: https://youtu.be/fSBVVt8U0dA

- `[M4-LI-02]` Voti sopra e sotto la media
	- Testo: Data la lista voti = [18, 25, 30, 22, 28, 19], calcola la media. Poi stampa i voti sopra la media e quelli sotto.
	- Soluzione:
		```python
		voti = [18, 25, 30, 22, 28, 19]
		somma = 0
		i = 0
		while i < len(voti):
		    somma = somma + voti[i]
		    i = i + 1
		media = somma / len(voti)
		print("Media:", media)
		sopra = []
		sotto = []
		i = 0
		while i < len(voti):
		    if voti[i] > media:
		        sopra.append(voti[i])
		    else:
		        sotto.append(voti[i])
		    i = i + 1
		print("Sopra:", sopra)
		print("Sotto:", sotto)
		```

- `[M4-LI-03]` Parole pari e dispari
	- Testo: Leggi parole fino a "fine". Costruisci due liste: parole di lunghezza pari e parole di lunghezza dispari. Stampa entrambe.
	- Soluzione:
		```python
		pari = []
		dispari = []
		parola = input("Parola: ")
		while parola != "fine":
		    if len(parola) % 2 == 0:
		        pari.append(parola)
		    else:
		        dispari.append(parola)
		    parola = input("Parola: ")
		print(pari)
		print(dispari)
		```

- `[M4-LI-04]` Palindromo con lista
	- Testo: Verifica se una parola è un palindromo convertendola in lista di caratteri e confrontando dal primo all'ultimo con due indici.
	- Soluzione:
		```python
		parola = input("Parola: ")
		caratteri = []
		i = 0
		while i < len(parola):
		    caratteri.append(parola[i])
		    i = i + 1
		sx = 0
		dx = len(caratteri) - 1
		palindromo = True
		while sx < dx:
		    if caratteri[sx] != caratteri[dx]:
		        palindromo = False
		    sx = sx + 1
		    dx = dx - 1
		if palindromo:
		    print("sì")
		else:
		    print("no")
		```

- `[M4-LI-05]` Lista senza duplicati
	- Testo: Leggi interi fino a 0. Stampa la lista senza duplicati, conservando l'ordine di prima apparizione. (Es.: 3 1 4 1 5 3 2 0 → [3, 1, 4, 5, 2])
	- Soluzione:
		```python
		unici = []
		n = int(input("Numero: "))
		while n != 0:
		    if n not in unici:
		        unici.append(n)
		    n = int(input("Numero: "))
		print(unici)
		```
	- Video: https://youtu.be/WDYxqzwCraY

- `[M4-LI-06]` Elementi in comune
	- Testo: Date due liste già definite, costruisci una terza lista con gli elementi in comune senza duplicati. (Es.: a=[1,3,5,7,3], b=[3,5,6,7] → [3,5,7])
	- Soluzione:
		```python
		a = [1, 3, 5, 7, 3]
		b = [3, 5, 6, 7]
		comuni = []
		i = 0
		while i < len(a):
		    if a[i] in b and a[i] not in comuni:
		        comuni.append(a[i])
		    i = i + 1
		print(comuni)
		```

- `[M4-LA-01]` Secondo massimo
	- Testo: Leggi interi fino a 0. Stampa il secondo massimo. Se ci sono meno di due valori distinti, stampa un messaggio apposito. (Es.: 5 3 9 7 9 0 → 7)
	- Soluzione:
		```python
		numeri = []
		n = int(input("Numero: "))
		while n != 0:
		    numeri.append(n)
		    n = int(input("Numero: "))
		if len(numeri) < 2:
		    print("meno di due valori")
		else:
		    massimo = numeri[0]
		    i = 1
		    while i < len(numeri):
		        if numeri[i] > massimo:
		            massimo = numeri[i]
		        i = i + 1
		    secondo = None
		    i = 0
		    while i < len(numeri):
		        if numeri[i] < massimo:
		            if secondo is None or numeri[i] > secondo:
		                secondo = numeri[i]
		        i = i + 1
		    if secondo is None:
		        print("nessun secondo massimo")
		    else:
		        print(secondo)
		```

- `[M4-LA-02]` Parola più lunga e più corta
	- Testo: Leggi parole fino a "fine". Stampa la parola più lunga e quella più corta. In caso di parità prendi la prima apparsa.
	- Soluzione:
		```python
		parole = []
		parola = input("Parola: ")
		while parola != "fine":
		    parole.append(parola)
		    parola = input("Parola: ")
		if len(parole) == 0:
		    print("nessuna parola")
		else:
		    piu_lunga = parole[0]
		    piu_corta = parole[0]
		    i = 1
		    while i < len(parole):
		        if len(parole[i]) > len(piu_lunga):
		            piu_lunga = parole[i]
		        if len(parole[i]) < len(piu_corta):
		            piu_corta = parole[i]
		        i = i + 1
		    print("Più lunga:", piu_lunga)
		    print("Più corta:", piu_corta)
		```

- `[M4-LA-03]` Coppie crescenti
	- Testo: Leggi interi fino a 0. Stampa tutte le coppie consecutive (lista[i], lista[i+1]) tali che il secondo è strettamente maggiore del primo. (Es.: 2 5 3 8 8 1 0 → (2,5) (3,8))
	- Soluzione:
		```python
		numeri = []
		n = int(input("Numero: "))
		while n != 0:
		    numeri.append(n)
		    n = int(input("Numero: "))
		i = 0
		while i < len(numeri) - 1:
		    if numeri[i + 1] > numeri[i]:
		        print("(" + str(numeri[i]) + ", " + str(numeri[i + 1]) + ")")
		    i = i + 1
		```

- `[M4-LA-04]` Conteggio occorrenze precedenti
	- Testo: Leggi parole fino a "fine". Per ogni parola stampa quante volte quella parola è già apparsa in precedenza nella lista. (Es.: gatto cane gatto gatto cane fine → gatto:0, cane:0, gatto:1, gatto:2, cane:1)
	- Soluzione:
		```python
		parole = []
		parola = input("Parola: ")
		while parola != "fine":
		    contatore = 0
		    i = 0
		    while i < len(parole):
		        if parole[i] == parola:
		            contatore = contatore + 1
		        i = i + 1
		    print(parola + ":" + str(contatore))
		    parole.append(parola)
		    parola = input("Parola: ")
		```

- `[M4-LA-05]` Lunghezza frase con split
	- Testo: Leggi una frase dall'input, usa split() per ottenere la lista delle parole e stampa quante parole contiene.
	- Soluzione:
		```python
		frase = input("Frase: ")
		parole = frase.split()
		print(len(parole))
		```

- `[M4-LA-06]` Inverti lista in place
	- Testo: Data una lista di interi, invertila senza crearne una nuova. Usa due indici (uno all'inizio, uno alla fine) e scambia gli elementi avvicinandoli al centro. (Es.: [1, 2, 3, 4, 5] → [5, 4, 3, 2, 1])
	- Soluzione:
		```python
		lista = [1, 2, 3, 4, 5]
		sx = 0
		dx = len(lista) - 1
		while sx < dx:
		    tmp = lista[sx]
		    lista[sx] = lista[dx]
		    lista[dx] = tmp
		    sx = sx + 1
		    dx = dx - 1
		print(lista)
		```

- `[M4-LA-07]` Zeri in fondo
	- Testo: Data una lista di interi che contiene alcuni zeri, sposta tutti gli zeri alla fine conservando l'ordine degli altri elementi, senza creare una nuova lista. (Es.: [0, 3, 0, 1, 5, 0, 2] → [3, 1, 5, 2, 0, 0, 0])
	- Soluzione:
		```python
		lista = [0, 3, 0, 1, 5, 0, 2]
		i = 0
		while i < len(lista):
		    if lista[i] == 0:
		        del lista[i]
		        lista.append(0)
		    else:
		        i = i + 1
		print(lista)
		```

- `[M4-LA-08]` Rotazione ciclica a sinistra
	- Testo: Data una lista e un intero k, ruota la lista di k posizioni verso sinistra in place, senza creare una nuova lista. (Es.: [1, 2, 3, 4, 5] con k=2 → [3, 4, 5, 1, 2])
	- Soluzione:
		```python
		lista = [1, 2, 3, 4, 5]
		k = 2
		i = 0
		while i < k:
		    primo = lista[0]
		    j = 0
		    while j < len(lista) - 1:
		        lista[j] = lista[j + 1]
		        j = j + 1
		    lista[-1] = primo
		    i = i + 1
		print(lista)
		```

- `[M4-LA-09]` Massimo e sua posizione
	- Testo: Leggi interi fino a 0 e salvali in una lista. Trova il valore massimo e stampa sia il valore sia l'indice in cui si trova nella lista. In caso di parità, prendi la prima occorrenza.
	- Soluzione:
		```python
		numeri = []
		n = int(input("Numero: "))
		while n != 0:
		    numeri.append(n)
		    n = int(input("Numero: "))
		if len(numeri) == 0:
		    print("lista vuota")
		else:
		    massimo = numeri[0]
		    pos = 0
		    i = 1
		    while i < len(numeri):
		        if numeri[i] > massimo:
		            massimo = numeri[i]
		            pos = i
		        i = i + 1
		    print("Valore:", massimo)
		    print("Posizione:", pos)
		```

- `[M4-LA-10]` Merge di due sequenze ordinate
	- Testo: Date due liste già ordinate in modo crescente (es. a=[1,3,5,7] e b=[2,4,6]), costruisci una terza lista che contiene tutti gli elementi in ordine crescente, senza usare sort(). (Suggerimento: usa due indici, uno per lista, e avanza quello con il valore minore.)
	- Soluzione:
		```python
		a = [1, 3, 5, 7]
		b = [2, 4, 6]
		merged = []
		i = 0
		j = 0
		while i < len(a) and j < len(b):
		    if a[i] <= b[j]:
		        merged.append(a[i])
		        i = i + 1
		    else:
		        merged.append(b[j])
		        j = j + 1
		while i < len(a):
		    merged.append(a[i])
		    i = i + 1
		while j < len(b):
		    merged.append(b[j])
		    j = j + 1
		print(merged)
		```



### 05 | Funzioni e procedure

- `[M5-TRACE-01]` Tracciare lo stato: somma(a, b)
	- Testo: Compila la tabella dello stato per il seguente programma.
	- Codice:
		```python
		def somma(a, b):
		    risultato = a + b
		    return risultato

		x = 3
		y = 7
		z = somma(x, y)
		print(z)
		```
	- Traccia: Soluzione
		| passo | riga | stato principale | stato funzione | output |
		|-------|------|-----------------|----------------|--------|
		| 1 | `x = 3` | x=3 | — | |
		| 2 | `y = 7` | x=3, y=7 | — | |
		| 3 | `z = somma(x, y)` | x=3, y=7 | a=3, b=7 | |
		| 4 | `risultato = a + b` | x=3, y=7 | a=3, b=7, risultato=10 | |
		| 5 | `return risultato` | x=3, y=7, z=10 | — | |
		| 6 | `print(z)` | x=3, y=7, z=10 | — | 10 |

- `[M5-TRACE-02]` Tracciare lo stato: is_pari(n)
	- Testo: Compila la tabella dello stato per il seguente programma.
	- Codice:
		```python
		def is_pari(n):
		    return n % 2 == 0

		x = 4
		if is_pari(x):
		    print("pari")
		else:
		    print("dispari")
		```
	- Traccia: Soluzione
		| passo | riga | stato principale | stato funzione | output |
		|-------|------|-----------------|----------------|--------|
		| 1 | `x = 4` | x=4 | — | |
		| 2 | `is_pari(x)` | x=4 | n=4 | |
		| 3 | `return n % 2 == 0` | x=4 | n=4 → True | |
		| 4 | `if True:` | x=4 | — | |
		| 5 | `print("pari")` | x=4 | — | pari |

- `[M5-ERR-01]` Errore: funzione doppio senza return
	- Testo: Il seguente programma vuole stampare il doppio di un numero, ma non funziona. Individua l'errore e correggilo.
	- Codice:
		```python
		def doppio(n):
		    n = n * 2

		x = 5
		print(doppio(x))
		```
	- Soluzione: La funzione modifica `n` localmente ma non ha `return`, quindi restituisce `None`. `print(None)` stampa `None` invece di 10.
		```python
		def doppio(n):
		    return n * 2

		x = 5
		print(doppio(x))
		```

- `[M5-ERR-02]` Errore: funzione somma con tre argomenti
	- Testo: Il seguente programma vuole calcolare la somma di tre numeri usando una funzione, ma produce un risultato sbagliato. Individua l'errore.
	- Codice:
		```python
		def somma(a, b):
		    return a + b

		print(somma(1, 2, 3))
		```
	- Soluzione: `somma` accetta 2 parametri ma viene chiamata con 3 → `TypeError`. Bisogna aggiungere il terzo parametro.
		```python
		def somma(a, b, c):
		    return a + b + c

		print(somma(1, 2, 3))
		```

- `[M5-ERR-03]` Errore: funzione che usa print invece di return
	- Testo: Il seguente programma vuole usare il risultato della funzione in una condizione, ma produce un errore. Spiega perché e correggilo.
	- Codice:
		```python
		def messaggio(nome):
		    print("Ciao, " + nome)

		if messaggio("Alice") == "Ciao, Alice":
		    print("ok")
		```
	- Soluzione: `messaggio()` usa `print` invece di `return`, quindi restituisce `None`. Il confronto `None == "Ciao, Alice"` è sempre `False` e il ramo `if` non viene mai eseguito. Bisogna usare `return`.
		```python
		def messaggio(nome):
		    return "Ciao, " + nome

		if messaggio("Alice") == "Ciao, Alice":
		    print("ok")
		```

- `[M5-SCOPE-01]` Scope: variabile locale non visibile fuori dalla funzione
	- Testo: Il seguente programma produce un errore oppure stampa qualcosa? Spiega cosa succede e perché.
	- Codice:
		```python
		def calcola():
		    x = 100
		    return x

		calcola()
		print(x)
		```
	- Soluzione: Il programma produce `NameError: name 'x' is not defined`. La variabile `x` è locale alla funzione `calcola()` e non è visibile nel programma principale. Inoltre il valore restituito da `calcola()` non viene salvato. Per correggere: `x = calcola()` poi `print(x)`.

- `[M5-SCOPE-02]` Scope: parametro locale non modifica la variabile principale
	- Testo: Cosa stampa il programma? Spiega perché n nel programma principale non cambia.
	- Codice:
		```python
		def azzera(n):
		    n = 0
		    return n

		n = 42
		azzera(n)
		print(n)
		```
	- Soluzione: Stampa `42`. Il parametro `n` dentro la funzione è una copia locale: modificarlo non cambia la variabile `n` nel programma principale. Il valore restituito da `azzera()` viene anche ignorato.

- `[M5-SCOPE-03]` Scope: raddoppio di variabile globale
	- Testo: Il programma vuole raddoppiare una variabile globale dall'interno di una funzione. Funziona come ci si aspetta? Riscrivilo nel modo corretto.
	- Codice:
		```python
		valore = 10

		def raddoppia():
		    return valore * 2

		print(raddoppia())
		```
	- Soluzione: Il programma stampa `20` e funziona, ma è fragile: la funzione dipende implicitamente dalla variabile globale `valore`. La versione corretta passa `valore` come parametro, rendendo la funzione indipendente.
		```python
		valore = 10

		def raddoppia(valore):
		    return valore * 2

		print(raddoppia(valore))
		```

- `[M5-SCOPE-04]` Scope: variabili locali e principali con stesso nome
	- Testo: Cosa stampa il programma? Compila la tabella dello stato.
	- Codice:
		```python
		def f(x):
		    y = x * 2
		    return y

		x = 3
		y = 10
		risultato = f(5)
		print(x, y, risultato)
		```
	- Traccia: Soluzione
		| passo | riga | stato principale | stato funzione | output |
		|-------|------|-----------------|----------------|--------|
		| 1 | `x = 3` | x=3 | — | |
		| 2 | `y = 10` | x=3, y=10 | — | |
		| 3 | `risultato = f(5)` | x=3, y=10 | x=5 | |
		| 4 | `y = x * 2` | x=3, y=10 | x=5, y=10 | |
		| 5 | `return y` | x=3, y=10, risultato=10 | — | |
		| 6 | `print(x, y, risultato)` | x=3, y=10, risultato=10 | — | 3 10 10 |

- `[M5-SCOPE-05]` Scope: errore di scope con variabili globali
	- Testo: Il programma contiene un errore di scope. Individualo e correggilo senza usare variabili globali.
	- Codice:
		```python
		def calcola_sconto():
		    return prezzo * sconto / 100

		prezzo = 80
		sconto = 20
		print(calcola_sconto())
		```
	- Soluzione: La funzione usa `prezzo` e `sconto` come variabili globali invece di riceverle come parametri. Sebbene funzioni, è un errore di design: la funzione dipende da variabili esterne e non è riutilizzabile autonomamente.
		```python
		def calcola_sconto(prezzo, sconto):
		    return prezzo * sconto / 100

		prezzo = 80
		sconto = 20
		print(calcola_sconto(prezzo, sconto))
		```

- `[M5-WRITE-01]` Perimetro rettangolo
	- Testo: Scrivi una funzione `perimetro_rettangolo(base, altezza)` che restituisce il perimetro di un rettangolo. (`perimetro_rettangolo(3, 5)` → 16)
	- Soluzione:
		```python
		def perimetro_rettangolo(base, altezza):
		    return 2 * (base + altezza)

		print(perimetro_rettangolo(3, 5))
		```

- `[M5-WRITE-02]` Divisibilità
	- Testo: Scrivi una funzione `divisibile(n, d)` che restituisce `True` se `n` è divisibile per `d`, `False` altrimenti. (`divisibile(12, 4)` → True; `divisibile(7, 3)` → False)
	- Soluzione:
		```python
		def divisibile(n, d):
		    return n % d == 0

		print(divisibile(12, 4))
		print(divisibile(7, 3))
		```

- `[M5-WRITE-03]` Massimo tra due numeri
	- Testo: Scrivi una funzione `massimo(a, b)` che restituisce il maggiore tra due numeri senza usare `max()`. (`massimo(3, 7)` → 7; `massimo(5, 5)` → 5)
	- Soluzione:
		```python
		def massimo(a, b):
		    if a >= b:
		        return a
		    else:
		        return b

		print(massimo(3, 7))
		print(massimo(5, 5))
		```

- `[M5-WRITE-04]` Conta vocali
	- Testo: Scrivi una funzione `conta_vocali(s)` che riceve una stringa e restituisce il numero di vocali che contiene. (`conta_vocali("informatica")` → 5)
	- Soluzione:
		```python
		def conta_vocali(s):
		    contatore = 0
		    i = 0
		    while i < len(s):
		        if s[i] in "aeiouAEIOU":
		            contatore += 1
		        i += 1
		    return contatore

		print(conta_vocali("informatica"))
		print(conta_vocali("ritmo"))
		```

- `[M5-WRITE-05]` Ripeti stringa
	- Testo: Scrivi una funzione `ripeti(s, n)` che restituisce la stringa `s` ripetuta `n` volte separata da spazi, senza usare l'operatore `*`. (`ripeti("ciao", 3)` → "ciao ciao ciao")
	- Soluzione:
		```python
		def ripeti(s, n):
		    risultato = s
		    i = 1
		    while i < n:
		        risultato = risultato + " " + s
		        i += 1
		    return risultato

		print(ripeti("ciao", 3))
		print(ripeti("ok", 1))
		```


- `[M5-WRITE-06]` Valore assoluto
	- Testo: Scrivi una funzione `valore_assoluto(n)` che restituisce il valore assoluto di un numero senza usare `abs()`. (`valore_assoluto(-3)` → 3; `valore_assoluto(5)` → 5)
	- Soluzione:
		```python
		def valore_assoluto(n):
		    if n < 0:
		        return -n
		    else:
		        return n

		print(valore_assoluto(-3))
		print(valore_assoluto(5))
		```

- `[M5-WRITE-07]` È una vocale?
	- Testo: Scrivi una funzione `vocale(c)` che riceve un carattere e restituisce `True` se è una vocale (maiuscola o minuscola), `False` altrimenti. (`vocale("a")` → True; `vocale("b")` → False)
	- Soluzione:
		```python
		def vocale(c):
		    return c in "aeiouAEIOU"

		print(vocale("a"))
		print(vocale("b"))
		```

- `[M5-WRITE-08]` Conversione temperatura
	- Testo: Scrivi una funzione `fahrenheit_in_celsius(f)` che converte gradi Fahrenheit in Celsius. Formula: `(f - 32) × 5 / 9`. (`fahrenheit_in_celsius(32)` → 0.0; `fahrenheit_in_celsius(212)` → 100.0)
	- Soluzione:
		```python
		def fahrenheit_in_celsius(f):
		    return (f - 32) * 5 / 9

		print(fahrenheit_in_celsius(32))
		print(fahrenheit_in_celsius(212))
		```

- `[M5-WRITE-09]` Minimo tra due numeri
	- Testo: Scrivi una funzione `minimo(a, b)` che restituisce il minore tra due numeri senza usare `min()`. (`minimo(3, 7)` → 3; `minimo(5, 5)` → 5)
	- Soluzione:
		```python
		def minimo(a, b):
		    if a <= b:
		        return a
		    else:
		        return b

		print(minimo(3, 7))
		print(minimo(5, 5))
		```

- `[M5-WRITE-10]` Media di tre numeri
	- Testo: Scrivi una funzione `media(a, b, c)` che restituisce la media aritmetica di tre numeri. (`media(4, 8, 6)` → 6.0)
	- Soluzione:
		```python
		def media(a, b, c):
		    return (a + b + c) / 3

		print(media(4, 8, 6))
		```

- `[M5-WRITE-11]` Somma delle cifre
	- Testo: Scrivi una funzione `somma_cifre(n)` che riceve un intero positivo e restituisce la somma delle sue cifre, senza convertirlo in stringa (usa `%` e `//`). (`somma_cifre(123)` → 6; `somma_cifre(9)` → 9)
	- Soluzione:
		```python
		def somma_cifre(n):
		    somma = 0
		    while n > 0:
		        somma = somma + n % 10
		        n = n // 10
		    return somma

		print(somma_cifre(123))
		print(somma_cifre(9))
		```

- `[M5-WRITE-12]` Numero primo
	- Testo: Scrivi una funzione `primo(n)` che restituisce `True` se `n` è primo, `False` altrimenti. (`primo(7)` → True; `primo(12)` → False)
	- Soluzione:
		```python
		def primo(n):
		    if n < 2:
		        return False
		    i = 2
		    while i < n:
		        if n % i == 0:
		            return False
		        i = i + 1
		    return True

		print(primo(7))
		print(primo(12))
		```

- `[M5-WRITE-13]` Massimo comun divisore
	- Testo: Scrivi una funzione `mcd(a, b)` che calcola il massimo comun divisore di due interi positivi usando l'algoritmo di Euclide: finché `b != 0`, sostituisci `a` con `b` e `b` con `a % b`. (`mcd(12, 8)` → 4; `mcd(7, 3)` → 1)
	- Soluzione:
		```python
		def mcd(a, b):
		    while b != 0:
		        a, b = b, a % b
		    return a

		print(mcd(12, 8))
		print(mcd(7, 3))
		```

- `[M5-WRITE-14]` Inverti stringa
	- Testo: Scrivi una funzione `inverti_stringa(s)` che restituisce la stringa invertita. (`inverti_stringa("ciao")` → "oaic"; `inverti_stringa("radar")` → "radar")
	- Soluzione:
		```python
		def inverti_stringa(s):
		    risultato = ""
		    i = len(s) - 1
		    while i >= 0:
		        risultato = risultato + s[i]
		        i = i - 1
		    return risultato

		print(inverti_stringa("ciao"))
		print(inverti_stringa("radar"))
		```

### 06 | Memoria e passaggio degli argomenti

- `[M6-TR-01]` Tracciamento: intero e riassegnazione
	- Codice:
		```python
			x = 5
			y = x
			y = y + 3
			print(x)
			print(y)
		```
	- Testo: Compila la tabella riga per riga e scrivi l'output prima di eseguire il codice.
	- Traccia: Soluzione
		| Passo | Codice sorgente | Stato del programma | Output |
		| ----- | --------------- | ------------------- | ------ |
		| 1 | x = 5 | x->5 | |
		| 2 | y = x | x,y->5 | |
		| 3 | y = y + 3 | x->5, y->8 | |
		| 4 | print(x) | | 5 |
		| 5 | print(y) | | 8 |

- `[M6-TR-02]` Tracciamento: lista con alias
	- Codice:
		```python
			a = [1, 2, 3]
			b = a
			b.append(4)
			print(a)
			print(b)
		```
	- Testo: Compila la tabella riga per riga e scrivi l'output prima di eseguire il codice.
	- Traccia: Soluzione
		| Passo | Codice sorgente | Stato del programma | Output |
		| ----- | --------------- | ------------------- | ------ |
		| 1 | a = [1, 2, 3] | a->[1,2,3] | |
		| 2 | b = a | a,b->[1,2,3] | |
		| 3 | b.append(4) | a,b->[1,2,3,4] | |
		| 4 | print(a) | | [1,2,3,4] |
		| 5 | print(b) | | [1,2,3,4] |

- `[M6-TR-03]` Tracciamento: lista con copia
	- Codice:
		```python
			a = [1, 2, 3]
			b = copia_lista(a)
			b.append(4)
			print(a)
			print(b)
		```
	- Testo: Compila la tabella riga per riga e scrivi l'output prima di eseguire il codice.
	- Traccia: Soluzione
		| Passo | Codice sorgente | Stato del programma | Output |
		| ----- | --------------- | ------------------- | ------ |
		| 1 | a = [1, 2, 3] | a->[1,2,3] | |
		| 2 | b = copia_lista(a) | a->[1,2,3], b->[1,2,3] | |
		| 3 | b.append(4) | a->[1,2,3], b->[1,2,3,4] | |
		| 4 | print(a) | | [1,2,3] |
		| 5 | print(b) | | [1,2,3,4] |


- `[M6-TR-04]` Tracciamento: riassegnazione vs modifica (trabocchetto)
	- Codice:
		```python
			a = [10, 20, 30]
			b = a
			a = [10, 20, 30]
			b.append(99)
			print(a)
			print(b)
		```
	- Testo: Attenzione: al passo 3 `a` viene riassegnata, non modificata. Compila la tabella e scrivi l'output.
	- Traccia: Soluzione
		| Passo | Codice sorgente | Stato del programma | Output |
		| ----- | --------------- | ------------------- | ------ |
		| 1 | a = [10, 20, 30] | a->[10,20,30] | |
		| 2 | b = a | a,b->[10,20,30] | |
		| 3 | a = [10, 20, 30] | a->[10,20,30], b->[10,20,30] | |
		| 4 | b.append(99) | a->[10,20,30], b->[10,20,30,99] | |
		| 5 | print(a) | | [10,20,30] |
		| 6 | print(b) | | [10,20,30,99] |


- `[M6-VF-01]` Vero o falso: intero condiviso
	- Testo: Dopo `x = 3` e `y = x`, eseguire `y = y + 1` cambia anche `x`.
	- Soluzione: **Falso.** Gli interi sono immutabili: `y = y + 1` sposta solo l'etichetta `y` su un nuovo oggetto. `x` rimane `3`.


- `[M6-VF-02]` Vero o falso: lista condivisa tramite alias
	- Testo: Dopo `a = [1, 2]` e `b = a`, eseguire `b.append(3)` cambia anche `a`.
	- Soluzione: **Vero.** `b = a` crea un alias. `b.append(3)` modifica in-place la lista condivisa, quindi `a` la vede.


- `[M6-VF-03]` Vero o falso: copia e identità
	- Testo: Dopo `a = [1, 2]` e `b = copia_lista(a)`, le due liste puntano allo stesso oggetto.
	- Soluzione: **Falso.** `copia_lista` crea un nuovo oggetto indipendente. Le due liste hanno valori uguali ma `a is b` è `False`.


- `[M6-VF-04]` Vero o falso: == implica is
	- Testo: Se `a == b` allora `a is b`.
	- Soluzione: **Falso.** Es.: `a = [1, 2]` e `b = [1, 2]` → `a == b` è `True` ma `a is b` è `False`.


- `[M6-VF-05]` Vero o falso: is implica ==
	- Testo: Se `a is b` allora `a == b`.
	- Soluzione: **Vero.** Se due variabili puntano allo stesso oggetto, il contenuto è necessariamente identico.


- `[M6-VF-06]` Vero o falso: due letterali lista
	- Testo: `a = [1, 2, 3]` e `b = [1, 2, 3]` scritti su righe separate creano due barattoli distinti.
	- Soluzione: **Vero.** Due letterali lista creano due oggetti distinti (diverso `id`). Il confronto `is` darebbe `False`.


- `[M6-B-01]` Alias in-place
	- Testo: Inizializza `a = [1, 2, 3]`. Crea `b` come alias di `a` e aggiungi `99`. Stampa entrambe e verifica con `is`.
	- Soluzione:
		```python
		a = [1, 2, 3]
		b = a
		b.append(99)
		print(a)
		print(b)
		print(a is b)
		```

- `[M6-B-02]` Copia indipendente
	- Testo: Inizializza `a = [1, 2, 3]`. Creane una copia indipendente `b` e aggiungi `99` alla copia. Stampa entrambe e verifica con `is`.
	- Soluzione:
		```python
		a = [1, 2, 3]
		b = []
		i = 0
		while i < len(a):
		    b.append(a[i])
		    i = i + 1
		b.append(99)
		print(a)
		print(b)
		print(a is b)
		```

- `[M6-B-03]` Lista pari e doppi
	- Testo: Leggi interi fino a 0. Salva in una lista solo i pari. Crea una copia e nella copia sostituisci ogni elemento con il suo doppio. Stampa entrambe.
	- Soluzione:
		```python
		pari = []
		n = int(input())
		while n != 0:
		    if n % 2 == 0:
		        pari.append(n)
		    n = int(input())
		doppi = []
		i = 0
		while i < len(pari):
		    doppi.append(pari[i])
		    i = i + 1
		i = 0
		while i < len(doppi):
		    doppi[i] = doppi[i] * 2
		    i = i + 1
		print("Lista pari:", pari)
		print("Lista doppi:", doppi)
		```

- `[M6-B-04]` Filtro sopra la media con marcatura
	- Testo: Leggi interi positivi fino a 0 (ignora i negativi). Salva in una lista. Crea una seconda lista con i valori sopra la media. Nella lista originale, sostituisci quei valori con -1. Stampa entrambe.
	- Soluzione:
		```python
		numeri = []
		n = int(input())
		while n != 0:
		    if n > 0:
		        numeri.append(n)
		    n = int(input())
		somma = 0
		i = 0
		while i < len(numeri):
		    somma = somma + numeri[i]
		    i = i + 1
		media = somma / len(numeri)
		filtrati = []
		i = 0
		while i < len(numeri):
		    if numeri[i] > media:
		        filtrati.append(numeri[i])
		        numeri[i] = -1
		    i = i + 1
		print("Lista originale:", numeri)
		print("Sopra la media:", filtrati)
		```

- `[M6-T-01]` Tracciamento con funzione void
	- Testo: Compila la tabella con due colonne stato.
	- Codice:
		```python
		def f(lista):
		    lista.append(99)

		x = [10, 20]
		f(x)
		print(x)
		```
	- Traccia: Soluzione
		| Passo | Codice | Stato - globale | Stato - funzione | Output |
		| ----- | ------ | --------------- | ---------------- | ------ |
		| 1 | x = [10, 20] | x->[10,20] | | |
		| 2 | f(x) | x->[10,20] | lista->x->[10,20] | |
		| 3 | lista.append(99) | x->[10,20,99] | lista->x->[10,20,99] | |
		| 4 | (ritorno) | x->[10,20,99] | - | |
		| 5 | print(x) | | | [10,20,99] |

- `[M6-T-02]` Tracciamento con funzione pura
	- Testo: Compila la tabella con due colonne stato.
	- Codice:
		```python
		def h(n):
		    n = n + 1
		    return n

		x = 5
		y = h(x)
		print(x)
		print(y)
		```
	- Traccia: Soluzione
		| Passo | Codice | Stato - globale | Stato - funzione | Output |
		| ----- | ------ | --------------- | ---------------- | ------ |
		| 1 | x = 5 | x->5 | | |
		| 2 | y = h(x) | x->5 | n->x->5 | |
		| 3 | n = n + 1 | x->5 | n->6 | |
		| 4 | return n | x->5 | - | |
		| 5 | (assegna y) | x->5, y->6 | | |
		| 6 | print(x) | | | 5 |
		| 7 | print(y) | | | 6 |

- `[M6-E-01]` massimo pura
	- Testo: Scrivi una funzione pura `massimo(lista)` che restituisce il valore massimo.
	- Soluzione:
		```python
		def massimo(lista):
		    m = lista[0]
		    i = 1
		    while i < len(lista):
		        if lista[i] > m:
		            m = lista[i]
		        i = i + 1
		    return m

		print(massimo([3, 1, 7, 2, 5]))
		```

- `[M6-E-02]` conta pura
	- Testo: Scrivi una funzione pura `conta(lista, valore)` che restituisce quante volte `valore` compare.
	- Soluzione:
		```python
		def conta(lista, valore):
		    contatore = 0
		    i = 0
		    while i < len(lista):
		        if lista[i] == valore:
		            contatore = contatore + 1
		        i = i + 1
		    return contatore

		print(conta([1, 2, 1, 3, 1], 1))
		```

- `[M6-E-03]` inverti pura
	- Testo: Scrivi una funzione pura `inverti(lista)` che restituisce una nuova lista con gli elementi in ordine inverso, senza modificare l'originale.
	- Soluzione:
		```python
		def inverti(lista):
		    risultato = []
		    i = len(lista) - 1
		    while i >= 0:
		        risultato.append(lista[i])
		        i = i - 1
		    return risultato

		dati = [1, 2, 3]
		print(inverti(dati))
		print(dati)
		```

- `[M6-E-04]` rimuovi_duplicati pura
	- Testo: Scrivi una funzione pura `rimuovi_duplicati(lista)` che restituisce una nuova lista senza ripetizioni (ordine di prima apparizione). `rimuovi_duplicati([1,2,1,3,2,4])` → `[1,2,3,4]`
	- Soluzione:
		```python
		def rimuovi_duplicati(lista):
		    risultato = []
		    i = 0
		    while i < len(lista):
		        if lista[i] not in risultato:
		            risultato.append(lista[i])
		        i = i + 1
		    return risultato

		print(rimuovi_duplicati([1, 2, 1, 3, 2, 4]))
		```

- `[M6-E-05]` unisci pura
	- Testo: Scrivi una funzione pura `unisci(a, b)` che restituisce una nuova lista con tutti gli elementi di `a` seguiti da quelli di `b`, senza modificare né `a` né `b`.
	- Soluzione:
		```python
		def unisci(a, b):
		    risultato = []
		    i = 0
		    while i < len(a):
		        risultato.append(a[i])
		        i = i + 1
		    i = 0
		    while i < len(b):
		        risultato.append(b[i])
		        i = i + 1
		    return risultato

		print(unisci([1, 2], [3, 4, 5]))
		```

- `[M6-E-06]` filtra_pari pura
	- Testo: Scrivi una funzione pura `filtra_pari(lista)` che restituisce una nuova lista contenente solo i numeri pari.
	- Soluzione:
		```python
		def filtra_pari(lista):
		    risultato = []
		    i = 0
		    while i < len(lista):
		        if lista[i] % 2 == 0:
		            risultato.append(lista[i])
		        i = i + 1
		    return risultato

		print(filtra_pari([1, 2, 3, 4, 5, 6]))
		```

- `[M6-E-07]` inverti_in_place void
	- Testo: Scrivi una funzione void `inverti_in_place(lista)` che inverte la lista modificandola direttamente.
	- Soluzione:
		```python
		def inverti_in_place(lista):
		    sx = 0
		    dx = len(lista) - 1
		    while sx < dx:
		        tmp = lista[sx]
		        lista[sx] = lista[dx]
		        lista[dx] = tmp
		        sx = sx + 1
		        dx = dx - 1

		dati = [1, 2, 3, 4, 5]
		inverti_in_place(dati)
		print(dati)
		```

- `[M6-E-08]` azzera_negativi void
	- Testo: Scrivi una funzione void `azzera_negativi(lista)` che sostituisce ogni negativo con `0` nella lista originale. `dati=[3,-1,7,-4,0]` → `[3,0,7,0,0]`
	- Soluzione:
		```python
		def azzera_negativi(lista):
		    i = 0
		    while i < len(lista):
		        if lista[i] < 0:
		            lista[i] = 0
		        i = i + 1

		dati = [3, -1, 7, -4, 0]
		azzera_negativi(dati)
		print(dati)
		```

- `[M6-E-09]` moltiplica_elementi void
	- Testo: Scrivi una funzione void `moltiplica_elementi(lista, fattore)` che moltiplica ogni elemento per `fattore` in-place. `dati=[1,2,3,4]`, `moltiplica_elementi(dati,3)` → `[3,6,9,12]`
	- Soluzione:
		```python
		def moltiplica_elementi(lista, fattore):
		    i = 0
		    while i < len(lista):
		        lista[i] = lista[i] * fattore
		        i = i + 1

		dati = [1, 2, 3, 4]
		moltiplica_elementi(dati, 3)
		print(dati)
		```

- `[M6-E-10]` aggiungi_tutti void
	- Testo: Scrivi una funzione void `aggiungi_tutti(destinazione, sorgente)` che aggiunge tutti gli elementi di `sorgente` in fondo a `destinazione` in-place. `a=[1,2,3]`, `b=[4,5,6]`, `aggiungi_tutti(a,b)` → `a=[1,2,3,4,5,6]`, `b=[4,5,6]`.
	- Soluzione:
		```python
		def aggiungi_tutti(destinazione, sorgente):
		    i = 0
		    while i < len(sorgente):
		        destinazione.append(sorgente[i])
		        i = i + 1

		a = [1, 2, 3]
		b = [4, 5, 6]
		aggiungi_tutti(a, b)
		print(a)
		print(b)
		```

- `[M6-L-01]` conta_sei
	- Testo: Scrivi una funzione `conta_sei(n)` che simula `n` lanci di un dado e restituisce quante volte è uscito `6`. `print(conta_sei(1000))` → es. 162
	- Soluzione:
		```python
		import random

		def conta_sei(n):
		    contatore = 0
		    i = 0
		    while i < n:
		        if random.randint(1, 6) == 6:
		            contatore = contatore + 1
		        i = i + 1
		    return contatore

		print(conta_sei(1000))
		```

- `[M6-L-02]` lista_casuale
	- Testo: Scrivi una funzione `lista_casuale(n, minimo, massimo)` che restituisce una lista di `n` interi casuali compresi tra `minimo` e `massimo`. `print(lista_casuale(5, 1, 100))` → es. `[47, 3, 82, 19, 61]`
	- Soluzione:
		```python
		import random

		def lista_casuale(n, minimo, massimo):
		    risultato = []
		    i = 0
		    while i < n:
		        risultato.append(random.randint(minimo, massimo))
		        i = i + 1
		    return risultato

		print(lista_casuale(5, 1, 100))
		```

- `[M6-L-03]` mischia / mescolata
	- Testo: Scrivi una funzione void `mischia(lista)` che mescola la lista in-place. Poi scrivi una funzione pura `mescolata(lista)` che restituisce una nuova lista mescolata senza modificare l'originale.
	- Soluzione:
		```python
		import random

		def mischia(lista):
		    random.shuffle(lista)

		def mescolata(lista):
		    copia = []
		    i = 0
		    while i < len(lista):
		        copia.append(lista[i])
		        i = i + 1
		    random.shuffle(copia)
		    return copia

		dati = [1, 2, 3, 4, 5]
		mischia(dati)
		print(dati)

		dati2 = [1, 2, 3, 4, 5]
		nuova = mescolata(dati2)
		print(dati2)
		print(nuova)
		```


### P2 | Prova intermedia 2 | prova=true

#### Esercizi-3 | Preparazione seconda prova

- `[EX-SIM3-ERR1]` raddoppia — funzione che non raddoppia
	- Testo: Il programma vuole stampare il doppio di ogni elemento della lista, ma la funzione restituisce la lista invariata. Individua e correggi l'errore.
	- Soluzione:
		```python
		# Bug: la funzione restituisce lista senza modificarla.
		# Correzione: iterare sugli elementi e moltiplicare ciascuno per 2.

		def raddoppia(lista):
		    i = 0
		    while i < len(lista):
		        lista[i] = lista[i] * 2
		        i = i + 1
		    return lista

		numeri = [1, 2, 3]
		numeri = raddoppia(numeri)
		print(numeri)   # [2, 4, 6]
		```

- `[EX-SIM3-ERR2]` sort() restituisce None
	- Testo: Il programma vuole ottenere la lista ordinata senza modificare l'originale. Spiega cosa stampa e perché, poi proponi la correzione.
	- Soluzione:
		```python
		# Bug: lista.sort() ordina in-place e restituisce None.
		# Quindi "originale" viene modificata e "ordinata" vale None.
		# Output reale:
		#   originale: [1, 2, 5, 8]
		#   ordinata: None
		#
		# Correzione: usare sorted() che restituisce una nuova lista.

		originale = [5, 2, 8, 1]
		ordinata = sorted(originale)
		print("originale:", originale)   # [5, 2, 8, 1]
		print("ordinata:", ordinata)     # [1, 2, 5, 8]
		```

- `[EX-SIM3-13]` Lista dei positivi con sentinella 0
	- Testo: Leggi numeri interi fino a 0. Stampa la lista dei soli numeri positivi inseriti.
	- Soluzione:
		```python
		positivi = []
		n = int(input("Numero: "))
		while n != 0:
		    if n > 0:
		        positivi.append(n)
		    n = int(input("Numero: "))
		print(positivi)
		```

- `[EX-SIM3-14]` minimo(lista)
	- Testo: Scrivi una funzione pura `minimo(lista)` che restituisce il valore minimo senza usare `min()`. Poi scrivi un programma con sentinella 0.
	- Soluzione:
		```python
		def minimo(lista):
		    m = lista[0]
		    i = 1
		    while i < len(lista):
		        if lista[i] < m:
		            m = lista[i]
		        i = i + 1
		    return m

		numeri = []
		n = int(input("Numero: "))
		while n != 0:
		    numeri.append(n)
		    n = int(input("Numero: "))
		print(minimo(numeri))
		```

- `[EX-SIM3-15]` conta_pari(lista)
	- Testo: Scrivi una funzione pura `conta_pari(lista)` che restituisce quanti numeri pari sono presenti nella lista.
	- Soluzione:
		```python
		def conta_pari(lista):
		    contatore = 0
		    i = 0
		    while i < len(lista):
		        if lista[i] % 2 == 0:
		            contatore = contatore + 1
		        i = i + 1
		    return contatore

		print(conta_pari([1, 2, 3, 4, 5, 6]))   # 3
		print(conta_pari([1, 3, 5]))             # 0
		```

- `[EX-SIM3-16]` sostituisci_negativi(lista, valore)
	- Testo: Scrivi una funzione void `sostituisci_negativi(lista, valore)` che sostituisce ogni elemento negativo con `valore`.
	- Soluzione:
		```python
		def sostituisci_negativi(lista, valore):
		    i = 0
		    while i < len(lista):
		        if lista[i] < 0:
		            lista[i] = valore
		        i = i + 1

		dati = [3, -1, 7, -4, 0]
		sostituisci_negativi(dati, 99)
		print(dati)   # [3, 99, 7, 99, 0]
		```

- `[EX-SIM3-17]` moltiplica(a, b) senza `*`
	- Testo: Scrivi una funzione pura `moltiplica(a, b)` che calcola `a * b` senza usare l'operatore `*`. Puoi assumere che `b` sia un intero non negativo.
	- Soluzione:
		```python
		def moltiplica(a, b):
		    risultato = 0
		    i = 0
		    while i < b:
		        risultato = risultato + a
		        i = i + 1
		    return risultato

		print(moltiplica(3, 4))   # 12
		print(moltiplica(7, 0))   # 0
		print(moltiplica(5, 1))   # 5
		```

- `[EX-SIM3-18]` contiene_pari(lista)
	- Testo: Scrivi una funzione pura `contiene_pari(lista)` che restituisce `True` se la lista contiene almeno un numero pari. Poi scrivi un programma con sentinella 0.
	- Soluzione:
		```python
		def contiene_pari(lista):
		    trovato = False
		    i = 0
		    while i < len(lista):
		        if lista[i] % 2 == 0:
		            trovato = True
		        i = i + 1
		    return trovato

		numeri = []
		n = int(input("Numero: "))
		while n != 0:
		    numeri.append(n)
		    n = int(input("Numero: "))
		if contiene_pari(numeri):
		    print("La lista contiene almeno un numero pari.")
		else:
		    print("La lista non contiene numeri pari.")
		```
	- Video: https://youtu.be/OLiRMmsr6jM

- `[EX-SIM3-19]` tutti_positivi(lista)
	- Testo: Scrivi una funzione pura `tutti_positivi(lista)` che restituisce `True` se tutti gli elementi sono strettamente positivi. Poi scrivi un programma con sentinella 0.
	- Soluzione:
		```python
		def tutti_positivi(lista):
		    tutti = True
		    i = 0
		    while i < len(lista):
		        if lista[i] <= 0:
		            tutti = False
		        i = i + 1
		    return tutti

		numeri = []
		n = int(input("Numero: "))
		while n != 0:
		    numeri.append(n)
		    n = int(input("Numero: "))
		print(tutti_positivi(numeri))
		```

- `[EX-SIM3-20]` e_ordinata(lista)
	- Testo: Scrivi una funzione pura `e_ordinata(lista)` che restituisce `True` se gli elementi sono in ordine non decrescente. Poi scrivi un programma con sentinella 0.
	- Soluzione:
		```python
		def e_ordinata(lista):
		    ordinata = True
		    i = 0
		    while i < len(lista) - 1:
		        if lista[i] > lista[i + 1]:
		            ordinata = False
		        i = i + 1
		    return ordinata

		numeri = []
		n = int(input("Numero: "))
		while n != 0:
		    numeri.append(n)
		    n = int(input("Numero: "))
		if e_ordinata(numeri):
		    print("La lista è ordinata.")
		else:
		    print("La lista non è ordinata.")
		```

### 07 | Ciclo for e iterazione


### 08 | File e formati strutturati


### P3 | Prova intermedia 3 | prova=true


### 09 | Dizionari, set e organizzazione dell'informazione


### 10 | Git e controllo di versione
